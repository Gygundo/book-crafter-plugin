---
phase: 05-formatter-docx-output
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - skills/orchestrator/SKILL.md
  - references/pipeline-stages.md
autonomous: true
requirements: [FMT-01, FMT-08]

must_haves:
  truths:
    - "Orchestrator Stage 5 invokes the formatter skill with the project directory after review gate approval"
    - "Pipeline state detection recognises output/*.docx as pipeline COMPLETE"
    - "Pipeline-stages.md documents the formatter's input, output, section architecture, and completion detection"
  artifacts:
    - path: "skills/orchestrator/SKILL.md"
      provides: "Stage 5 execution detail"
      contains: "Stage 5"
    - path: "references/pipeline-stages.md"
      provides: "Updated Stage 5 documentation"
      contains: "Document Section Architecture"
  key_links:
    - from: "skills/orchestrator/SKILL.md"
      to: "skills/formatter/SKILL.md"
      via: "invokes book-crafter:formatter skill"
      pattern: "book-crafter:formatter"
    - from: "skills/orchestrator/SKILL.md"
      to: "output/*.docx"
      via: "state detection checks for .docx existence"
      pattern: "output/.*\\.docx"
---

<objective>
Wire the formatter skill into the orchestrator's Stage 5 execution and update pipeline-stages.md with formatter implementation details.

Purpose: The orchestrator needs to know how to invoke the formatter after the review gate approves, and pipeline documentation needs to reflect the actual implementation.
Output: Updated `skills/orchestrator/SKILL.md` (Stage 5 section) and `references/pipeline-stages.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-formatter-docx-output/05-RESEARCH.md
@.planning/phases/05-formatter-docx-output/05-01-SUMMARY.md

@skills/orchestrator/SKILL.md
@references/pipeline-stages.md
@skills/formatter/SKILL.md

<interfaces>
<!-- From orchestrator SKILL.md, the relevant existing sections: -->

Stage 5 is currently mentioned in:
- Section 1 Pipeline Overview: "Stage 5: FORMAT (formatter skill) Output: output/[Book Title].docx"
- Section 3 State Detection: "Check for output/*.docx -> If exists: pipeline is COMPLETE"
- Section 4 Status Dashboard: "[ ] Stage 5: Formatting (formatter) [ ] .docx generation"
- Section 5 Stage Execution Step 3: "Stage 5 (Format): Invoke the formatter skill with all edited chapters"
- Section 5 Stage 4 Review Gate: "On Option 1 (Approve): Proceed to Stage 5"

From pipeline-stages.md Stage 5 section (current):
```
## Stage 5: Format (formatter skill)
**Input:** All `edited/ch*-final.md` files + formatting guide
**Output:** `output/[Book Title].docx`
**Parallel:** No -- single document generation
Produces professional .docx with:
- Front matter (half title, full title page, copyright, dedication, TOC)
- Chapter headings with page breaks
- Page numbers (Page X of Y)
- Back matter (about the author, scripture index, glossary)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand orchestrator Stage 5 execution with formatter invocation detail</name>
  <files>skills/orchestrator/SKILL.md</files>
  <read_first>
    - skills/orchestrator/SKILL.md (current full content -- Stage 5 references scattered across sections 1, 3, 4, 5)
    - skills/formatter/SKILL.md (the now-complete formatter skill -- need to know invocation interface)
  </read_first>
  <action>
Add a `#### Stage 5: Format` subsection under "### Stage-Specific Orchestration Notes" (Section 5) in the orchestrator SKILL.md. Place it after the Stage 4 subsection. This follows the same pattern as Stages 1-4 which each have detailed subsections.

The Stage 5 subsection must contain:

**Step 1: Verify readiness**
1. Confirm Stage 4 is COMPLETE: all `edited/ch[NN]-final.md` files exist AND `reports/consistency-report.md` exists AND no `<!-- REVISION IN PROGRESS -->` marker
2. Confirm `book-dna.md` exists in the project directory
3. Create `output/` directory if it does not exist: `mkdir -p [project_directory]/output`

**Step 2: Invoke the formatter**
Invoke the `book-crafter:formatter` skill with argument:
- Project directory path: `[project_directory]`

The formatter will:
1. Read `book-dna.md` for metadata (title, subtitle, author, chapter count, key terms, style rules)
2. Read `voice-profile.md` for spelling conventions
3. Read all `edited/ch[NN]-final.md` files
4. Generate a Node.js script using docx-js that assembles the complete .docx
5. Execute the script to produce `output/[Book Title].docx`

**Step 3: Verify output**
After the formatter returns:
1. Check that `output/` directory contains a `.docx` file: `ls output/*.docx 2>/dev/null`
2. Verify the file is > 0 bytes: `test -s output/*.docx`
3. Report file size: `ls -lh output/*.docx`
4. If validation script is available, run it: `python scripts/office/validate.py output/*.docx` (optional -- do not fail if script not found)

**Step 4: Report completion**
Display:
```
## Pipeline Complete: [Book Title]

Your book has been formatted and exported.

Output: [project_directory]/output/[Book Title].docx
Size: [file size]
Chapters: [N]

Front matter: Half title, Title page, Copyright, Dedication, Table of Contents
Back matter: About the Author, Scripture Index, Glossary

Note: When you open the .docx in Word, you may see "Update fields?" -- click Yes to populate the Table of Contents.

The book pipeline is now complete. You can:
1. Open the .docx in Microsoft Word or Google Docs
2. Request revisions to specific chapters (use Mode 5)
3. Start a new book project
```

Also update the **Status Dashboard** (Section 4) to show the completed format:
```
[x] Stage 5: Formatting (formatter)
    Generated: [date] | File: [filename] | Size: [size]
```

No changes needed to Section 3 (State Detection) since `output/*.docx` detection is already implemented.
  </action>
  <verify>
    <automated>grep -q "#### Stage 5: Format" skills/orchestrator/SKILL.md && grep -q "book-crafter:formatter" skills/orchestrator/SKILL.md && grep -q "output/.*\.docx" skills/orchestrator/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - skills/orchestrator/SKILL.md contains "#### Stage 5: Format" subsection under Stage-Specific Orchestration Notes
    - skills/orchestrator/SKILL.md contains "book-crafter:formatter" as the skill invocation
    - skills/orchestrator/SKILL.md contains "mkdir -p" for output directory creation
    - skills/orchestrator/SKILL.md contains verification step checking for .docx file existence
    - skills/orchestrator/SKILL.md contains "Pipeline Complete" completion message
    - skills/orchestrator/SKILL.md contains "Update fields" note about TOC
    - skills/orchestrator/SKILL.md Stage 5 references are consistent (sections 1, 3, 4, 5 all align)
    - The [STUB] marker check in Section 5 Step 2 will no longer block Stage 5 since formatter is now implemented
  </acceptance_criteria>
  <done>Orchestrator knows exactly how to invoke the formatter, verify its output, and present completion to the user</done>
</task>

<task type="auto">
  <name>Task 2: Update pipeline-stages.md with formatter implementation details</name>
  <files>references/pipeline-stages.md</files>
  <read_first>
    - references/pipeline-stages.md (current content -- Stage 5 is a brief placeholder)
    - skills/formatter/SKILL.md (complete formatter skill -- extract the actual implementation details)
  </read_first>
  <action>
Expand the Stage 5 section in `references/pipeline-stages.md` from its current brief description to match the detail level of Stage 4 (which has passes, rolling window, review gate, revision workflow documented).

Replace the current Stage 5 content with:

```markdown
## Stage 5: Format (formatter skill)

**Input:** All `edited/ch*-final.md` files + `book-dna.md` + `voice-profile.md`
**Output:** `output/[Book Title].docx`
**Parallel:** No -- single document generation

### Document Section Architecture

The generated .docx contains multiple sections with different page numbering:

1. **Half Title Page** -- centered title only, no headers/footers
2. **Full Title Page** -- title, subtitle, author name, no headers/footers
3. **Copyright Page** -- copyright notice, scripture permission, ISBN placeholder
4. **Dedication Page** -- placeholder for user-supplied dedication
5. **Table of Contents** -- auto-generated from chapter headings (HeadingLevel.HEADING_1), roman numeral page numbers
6. **Body (all chapters)** -- single section, chapters separated by pageBreakBefore, arabic page numbers restart at 1, header with book title, footer with "Page X of Y"
7. **About the Author** -- author bio or placeholder
8. **Scripture Index** -- auto-extracted from chapter content and METADATA blocks, sorted by canonical Bible book order
9. **Glossary** -- from Book DNA Key Terms table

### Typography

- Font: Georgia throughout
- Body: 12pt, 1.5 line spacing
- Chapter headings: 24pt bold, page break before
- Page size: US Letter (12240 x 15840 DXA)
- Margins: 1.5" top/bottom, 1" sides

### Auto-Extraction

- **Scripture Index:** Extracted from edited chapter METADATA `scriptures_used` fields + regex scan of chapter body. Deduplicated and sorted by canonical Bible book order (Genesis through Revelation).
- **Glossary:** Extracted from Book DNA Key Terms and Jargon table. Formatted as a two-column table (Term | Definition).

### Completion Detection

Stage 5 is complete when `output/` directory contains a `.docx` file.
```

Also update the Stage Completion Detection table at the bottom to ensure the Format row reads:
```
| Format | `output/` dir contains `.docx` file | `output/[Title].docx` |
```
(This should already match but verify.)
  </action>
  <verify>
    <automated>grep -q "Document Section Architecture" references/pipeline-stages.md && grep -q "Scripture Index" references/pipeline-stages.md && grep -q "canonical Bible book order" references/pipeline-stages.md && grep -q "Georgia" references/pipeline-stages.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - references/pipeline-stages.md Stage 5 section contains "### Document Section Architecture" heading
    - references/pipeline-stages.md contains all 9 document sections listed (Half Title through Glossary)
    - references/pipeline-stages.md contains "### Typography" heading with Georgia font, 12pt body, 24pt headings
    - references/pipeline-stages.md contains "### Auto-Extraction" heading documenting scripture index and glossary sources
    - references/pipeline-stages.md contains "canonical Bible book order" reference
    - references/pipeline-stages.md Stage Completion Detection table has Format row with "output/" and ".docx"
    - references/pipeline-stages.md Input line lists all three sources: edited files, book-dna.md, voice-profile.md
  </acceptance_criteria>
  <done>Pipeline-stages.md documents the actual formatter implementation: section architecture, typography choices, auto-extraction logic, and completion detection</done>
</task>

</tasks>

<verification>
1. Orchestrator Stage 5 section follows the same pattern as Stages 1-4 (step-by-step with verification)
2. Pipeline-stages.md Stage 5 matches the detail level of Stage 4
3. State detection for pipeline COMPLETE is consistent between orchestrator and pipeline-stages.md
</verification>

<success_criteria>
- Orchestrator can invoke the formatter and verify its output without ambiguity
- Pipeline documentation fully reflects the formatter's actual implementation
- Pipeline completion flow is end-to-end: Stage 4 approve -> Stage 5 format -> COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/05-formatter-docx-output/05-02-SUMMARY.md`
</output>
