---
phase: 11-distribution-packaging
plan: 06
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05]
files_modified:
  - .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md
  - fixtures/tiny-book/expected-captivation-score.txt
autonomous: false
requirements: [PKG-08]
must_haves:
  truths:
    - "`claude plugin validate .` exits 0 with zero warnings on the repo state"
    - "`bash scripts/release.sh` exits 0 and produces dist/book-crafter-v1.1.0.zip"
    - "`/book-crafter:sample` runs end-to-end, produces a valid .docx, and prints SAMPLE PASS"
    - "Smoke-test log committed to .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md"
    - "expected-captivation-score.txt recalibrated if first sample run diverges significantly from 8"
  artifacts:
    - path: ".planning/phases/11-distribution-packaging/11-SMOKE-TEST.md"
      provides: "Record of validate + release.sh + sample run outputs with timestamps"
      min_lines: 30
  key_links:
    - from: "claude plugin validate ."
      to: ".claude-plugin/plugin.json + marketplace.json + skills/*/SKILL.md + agents/*.md"
      via: "Official validator CLI"
      pattern: "plugin validate"
    - from: "/book-crafter:sample"
      to: "fixtures/tiny-book/run/final/*.docx"
      via: "Full pipeline execution"
      pattern: "SAMPLE PASS"
---

<objective>
Final verification of Phase 11: run `claude plugin validate .`, run `scripts/release.sh` end-to-end, and run `/book-crafter:sample` end-to-end. Record all three outputs. Recalibrate `expected-captivation-score.txt` if the first real run diverges from the starting value of 8.

Purpose: PKG-08 closes the phase with a clean validate, a clean release zip, and a green smoke-test — the three observable gates in the phase success criteria. This plan runs AFTER all build plans; it executes the system we built, not more building.
Output: `11-SMOKE-TEST.md` log file; possibly an updated threshold in `expected-captivation-score.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-distribution-packaging/11-CONTEXT.md
@.planning/phases/11-distribution-packaging/11-RESEARCH.md
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
@scripts/release.sh
@skills/sample/SKILL.md
@fixtures/tiny-book/brief.md
@fixtures/tiny-book/expected-captivation-score.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `claude plugin validate .` and capture output</name>
  <read_first>
    - .planning/phases/11-distribution-packaging/11-RESEARCH.md §"Validate Command" (exit codes, warnings vs errors)
    - .claude-plugin/plugin.json
    - .claude-plugin/marketplace.json
    - skills/*/SKILL.md frontmatter (all shipped skills)
    - agents/chapter-writer.md, agents/chapter-editor.md (frontmatter)
  </read_first>
  <files>.planning/phases/11-distribution-packaging/11-SMOKE-TEST.md</files>
  <action>
    **Precondition check:** Confirm `command -v claude` returns a path. If not, the executor MUST stop and notify the user — Phase 11 success criterion #2 (`claude plugin validate .` passes cleanly) cannot be satisfied without the CLI. This is NOT a checkpoint (no user input needed) — it's a hard precondition.

    **Run:**
    ```
    claude plugin validate . 2>&1 | tee /tmp/phase11-validate.log
    echo "EXIT=$?"
    ```

    **Expected:**
    - Exit code 0
    - Zero errors
    - Zero warnings (per SC-2: "passes cleanly with no warnings")

    **Handling warnings:** If validate prints warnings (non-blocking per research), fix them before proceeding:
    - Missing `description` in any SKILL.md frontmatter → add one
    - Non-kebab-case name → rename (but the only new skill is `sample`, already kebab-case)
    - Missing author in marketplace → already present (Plan 03)

    Errors (blocking) mean a schema bug in Plan 03 or a corrupt skill frontmatter. Halt and repair the offending file before continuing.

    **Create** `.planning/phases/11-distribution-packaging/11-SMOKE-TEST.md` with initial content:

    ```markdown
    # Phase 11 Smoke Test Log

    **Run:** 2026-04-XX
    **Executor:** Claude Code session
    **Purpose:** PKG-08 — record the three final gates of Phase 11 (validate, release.sh, sample).

    ## Gate 1: `claude plugin validate .`

    **Command:** `claude plugin validate .`
    **Exit code:** 0
    **Warnings:** 0
    **Errors:** 0

    <details><summary>Full output</summary>

    ```
    <paste /tmp/phase11-validate.log content here>
    ```

    </details>

    **Result:** PASS / FAIL
    ```

    Replace `<paste /tmp/phase11-validate.log content here>` with the real captured output. Replace `PASS / FAIL` with the chosen one.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md && grep -q 'Gate 1' .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md && grep -q 'claude plugin validate' .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md && (command -v claude >/dev/null && claude plugin validate . >/dev/null 2>&1 && echo "validate OK" || echo "validate unavailable or failed")</automated>
  </verify>
  <acceptance_criteria>
    - `claude` CLI is on PATH (precondition)
    - `claude plugin validate .` exits 0
    - Zero warnings in output (or warnings fixed before proceeding)
    - `11-SMOKE-TEST.md` created with Gate 1 section containing the real captured output
  </acceptance_criteria>
  <done>Validate gate green. Plan 02 success criterion #2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Run `bash scripts/release.sh` and append result to smoke-test log</name>
  <read_first>
    - scripts/release.sh
    - .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md (append, don't overwrite)
  </read_first>
  <files>.planning/phases/11-distribution-packaging/11-SMOKE-TEST.md</files>
  <action>
    **Clean slate:** Remove any stale `dist/` content from Plan 04's verification run:
    ```
    rm -rf dist/
    ```

    **Run:**
    ```
    bash scripts/release.sh 2>&1 | tee /tmp/phase11-release.log
    echo "EXIT=$?"
    ```

    **Expected:**
    - Exit code 0
    - All 9 gates report OK (Gate 5 may WARN if `claude` CLI is missing from the subshell's PATH — acceptable per D-26)
    - `dist/book-crafter-v1.1.0.zip` exists
    - Size < 5 MB
    - No `/Users/David` hits
    - Manifest listing (Gate 9) shows only whitelisted entries

    **Spot-check the zip contents (beyond what release.sh already checks):**
    ```
    unzip -l dist/book-crafter-v1.1.0.zip | head -40
    unzip -p dist/book-crafter-v1.1.0.zip book-crafter/.claude-plugin/plugin.json | grep '"name"'
    unzip -p dist/book-crafter-v1.1.0.zip book-crafter/.claude-plugin/marketplace.json | grep '"name"'
    unzip -p dist/book-crafter-v1.1.0.zip book-crafter/README.md | head -5
    ```

    Verify: staging plugin.json says `"name": "book-crafter"`, marketplace.json says `"name": "book-crafter-plugin"` and plugin entry `"name": "book-crafter"`, README has the Requires line.

    **Append** to `11-SMOKE-TEST.md`:

    ```markdown
    ## Gate 2: `bash scripts/release.sh`

    **Command:** `rm -rf dist/ && bash scripts/release.sh`
    **Exit code:** 0
    **Zip:** dist/book-crafter-v1.1.0.zip
    **Size:** <bytes> (<kilobytes> KB, <percent>% of 5MB cap)
    **Gate results:**
      - Gate 1 (version parse): OK
      - Gate 2 (CHANGELOG): OK
      - Gate 3 (whitelist copy): OK
      - Gate 4 (name rewrite): OK
      - Gate 5 (claude validate): OK / WARN-skipped
      - Gate 6 (zip): OK
      - Gate 7 (size): OK
      - Gate 8 (personal-path grep): OK
      - Gate 9 (manifest print): OK

    **Zip spot-check:**
    - plugin.json in staging: `"name": "book-crafter"`
    - Repo plugin.json: `"name": "book-crafter-dev"` (untouched)
    - marketplace.json top-level name: `"book-crafter-plugin"`

    <details><summary>Full release.sh output</summary>

    ```
    <paste /tmp/phase11-release.log>
    ```

    </details>

    **Result:** PASS / FAIL
    ```
  </action>
  <verify>
    <automated>rm -rf dist/ && bash scripts/release.sh && test -f dist/book-crafter-v1.1.0.zip && SIZE=$(stat -f%z dist/book-crafter-v1.1.0.zip 2>/dev/null || stat -c%s dist/book-crafter-v1.1.0.zip) && test "$SIZE" -lt 5242880 && unzip -p dist/book-crafter-v1.1.0.zip book-crafter/.claude-plugin/plugin.json | grep -q '"name": "book-crafter"' && grep -q 'Gate 2' .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/release.sh` exits 0
    - `dist/book-crafter-v1.1.0.zip` produced, size < 5 MB
    - Staging plugin.json in zip has name `book-crafter`
    - Repo plugin.json still has name `book-crafter-dev`
    - `11-SMOKE-TEST.md` has a Gate 2 section with captured output
  </acceptance_criteria>
  <done>Release.sh gate green. Phase success criterion #3 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Checkpoint): Run `/book-crafter:sample` end-to-end and verify PASS</name>
  <what-built>
    The sample skill (`skills/sample/SKILL.md`) and its fixture (`fixtures/tiny-book/brief.md`, `expected-captivation-score.txt`) are in place. Release.sh has built a clean zip. This checkpoint runs the actual user-facing demo end-to-end — it is the first time the full pipeline touches the fixture, and the result determines whether `expected-captivation-score.txt` needs recalibration.
  </what-built>
  <how-to-verify>
    **1. Clean slate.** Remove any prior run artefacts:
    ```
    rm -rf fixtures/tiny-book/run/
    ```

    **2. Invoke the sample skill.** In a Claude Code session, run:
    ```
    /book-crafter:sample
    ```

    **3. Wait for completion.** The full pipeline (outline → research → write → edit → enrich → format) should complete in ≤ 5 minutes per SC-4. If it runs longer, record the actual duration — the budget may need widening or the fixture may need trimming.

    **4. Verify the output.**
    - [ ] A `.docx` exists at `fixtures/tiny-book/run/final/<name>.docx`
    - [ ] Sample skill prints a line matching: `SAMPLE PASS — .docx at fixtures/tiny-book/run/final/<name>.docx, captivation N/14 (threshold 8)`
    - [ ] If the line is `SAMPLE FAIL`, read `fixtures/tiny-book/run/reports/consistency-report.md` to diagnose
    - [ ] Open the `.docx` briefly to confirm it is a real Word file (not a corrupt or empty one)

    **5. Recalibrate threshold (per D-07 and 11-RESEARCH.md Open Question #1):**
    - If captivation N is **11 or higher**, bump `fixtures/tiny-book/expected-captivation-score.txt` from `8` to `9` (leaves regression headroom)
    - If captivation N is **6 or lower**, the fixture or pipeline is broken — halt and report. Do NOT lower the threshold to paper over a broken pipeline.
    - If N is **7, 8, 9, or 10**, keep the threshold at `8`

    **6. Re-invocation smoke test (optional but recommended — validates D-11 fresh-mode branch):**
    Invoke `/book-crafter:sample` a second time. The skill should detect `fixtures/tiny-book/run/` and include "start fresh" in its orchestrator invocation, triggering Mode 6 wipe before re-running. Verify a SECOND `SAMPLE PASS` line.

    **7. Append to smoke-test log** (this section of `11-SMOKE-TEST.md`):

    ```markdown
    ## Gate 3: `/book-crafter:sample` end-to-end

    **First run:**
    - Duration: <minutes>
    - PASS/FAIL: PASS
    - Captivation: N/14 (threshold 8)
    - .docx path: fixtures/tiny-book/run/final/<name>.docx
    - .docx size: <bytes>

    **Threshold recalibration:**
    - Starting value: 8
    - First-run captivation: N
    - Decision: keep / bump to 9 / halt (broken pipeline)

    **Re-invocation (D-11 validation):**
    - Directory `fixtures/tiny-book/run/` existed: yes
    - Fresh mode triggered: yes/no (look for "start fresh" in orchestrator invocation trace)
    - Second run PASS/FAIL: PASS
    - Second captivation: N2/14

    **Result:** PASS / FAIL
    ```
  </how-to-verify>
  <resume-signal>
    Type "sample pass" after the first run emits SAMPLE PASS and the `.docx` is verified. If FAIL, type the failure reason and Claude will diagnose from `consistency-report.md`. If the pipeline exceeds 5 minutes, type "sample slow" with the duration — this may require a Phase 11 addendum plan to trim the fixture.
  </resume-signal>
  <acceptance_criteria>
    - `fixtures/tiny-book/run/final/*.docx` exists and opens as a valid Word file
    - Sample skill emitted `SAMPLE PASS` with captivation ≥ threshold
    - First-run duration ≤ 5 minutes (SC-4)
    - `expected-captivation-score.txt` is either unchanged (7–10) or bumped to 9 (≥11); NOT lowered
    - `11-SMOKE-TEST.md` has a Gate 3 section documenting both the first run and (optionally) the re-invocation
    - Fresh-mode re-invocation validated if David had time to run it twice
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Finalise smoke-test log and mark Phase 11 validation nyquist_compliant</name>
  <read_first>
    - .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md (accumulated Gates 1-3)
    - .planning/phases/11-distribution-packaging/11-VALIDATION.md (current frontmatter)
  </read_first>
  <files>
    .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md
  </files>
  <action>
    **Append** a final summary section to `11-SMOKE-TEST.md`:

    ```markdown
    ## Phase 11 Verification Summary

    | Gate | Requirement | Status |
    |------|-------------|--------|
    | 1 | `claude plugin validate .` clean (PKG-08, SC-2) | PASS |
    | 2 | `scripts/release.sh` produces <5MB zip with no PII (PKG-07, SC-3) | PASS |
    | 3 | `/book-crafter:sample` runs end-to-end in ≤5 min (PKG-09, SC-4) | PASS |
    | — | README has 3-command install block (PKG-03, SC-1) | PASS (Plan 02 Task 2) |
    | — | README zero "Claude Desktop" references (SC-5) | PASS (Plan 02 Task 2) |

    All Phase 11 success criteria observable in the shipped repo state.

    **Phase 11 → Phase 12 handoff:**
    - `evidence/eternally-secure-ch1-before.md` baseline freeze — Phase 12 GATE-01
    - Fresh-install smoke test (uninstall dev, install from marketplace) — Phase 12 GATE-06
    - README capability language evidence-anchoring — Phase 12 GATE-07
    - `git tag v1.1.0` — Phase 12 GATE-09 (blocked on GATE-08 ship decision)

    Phase 11 does NOT tag, does NOT finalise README capability line, does NOT run the Eternally Secure re-run. Those are Phase 12 owned.
    ```

    **Note on 11-VALIDATION.md:** The CONTEXT instruction mentioned updating `11-VALIDATION.md` to set `nyquist_compliant: true`. Leave that to the execute-phase orchestrator post-plan completion — this plan's in-scope artefact is the smoke-test log. (If the executor chooses to update the validation frontmatter here, it is acceptable; verify that every task in Plans 01-06 has an `<automated>` entry before flipping the flag.)
  </action>
  <verify>
    <automated>test -f .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md && grep -q 'Phase 11 Verification Summary' .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md && grep -q 'PASS' .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md && test "$(wc -l < .planning/phases/11-distribution-packaging/11-SMOKE-TEST.md)" -ge 30</automated>
  </verify>
  <acceptance_criteria>
    - `11-SMOKE-TEST.md` is ≥ 30 lines
    - Contains verification summary table
    - All three gates show PASS
    - Handoff section to Phase 12 documented
  </acceptance_criteria>
  <done>Phase 11 closes with all 5 ROADMAP success criteria observably met. Phase 12 can start.</done>
</task>

</tasks>

<verification>
- Three real runs recorded: validate, release.sh, sample
- All three green
- Threshold recalibrated if needed (honest calibration, not cheating)
- Smoke-test log documents handoff to Phase 12
</verification>

<success_criteria>
All five ROADMAP Phase 11 success criteria pass on the committed repo state:
1. README has the three-command install block verbatim (Plan 02)
2. `claude plugin validate .` clean (this plan, Task 1)
3. `scripts/release.sh` produces <5MB zip with no `/Users/David` (this plan, Task 2)
4. `/book-crafter:sample` runs end-to-end in ≤5 minutes producing a valid `.docx` (this plan, Task 3)
5. Zero "Claude Desktop" references in README (Plan 02 grep verified)

Phase 11 complete. Phase 12 hard gate starts next.
</success_criteria>

<output>
After completion, create `.planning/phases/11-distribution-packaging/11-06-SUMMARY.md` per the standard summary template, linking to `11-SMOKE-TEST.md` for the raw run outputs.
</output>
