---
phase: 11-distribution-packaging
plan: 05
type: execute
wave: 3
depends_on: [11-01]
files_modified:
  - skills/sample/SKILL.md
autonomous: true
requirements: [PKG-09]
must_haves:
  truths:
    - "/book-crafter:sample is a valid SKILL.md with correct frontmatter"
    - "Sample skill reads fixture from ${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/"
    - "Sample skill detects re-invocation via fixtures/tiny-book/run/ existence (D-11)"
    - "Sample skill first run omits 'start fresh'; re-run includes it"
    - "Sample skill prints exactly one PASS or FAIL summary line per D-12"
    - "Sample skill exit code matches pass/fail per D-13"
  artifacts:
    - path: "skills/sample/SKILL.md"
      provides: "One-command end-to-end demo skill"
      min_lines: 60
  key_links:
    - from: "skills/sample/SKILL.md"
      to: "skills/orchestrator/SKILL.md"
      via: "Invoke orchestrator skill programmatically with brief path + spiritual-default voice"
      pattern: "orchestrator"
    - from: "skills/sample/SKILL.md"
      to: "fixtures/tiny-book/brief.md"
      via: "${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/brief.md"
      pattern: "fixtures/tiny-book/brief\\.md"
    - from: "skills/sample/SKILL.md"
      to: "fixtures/tiny-book/expected-captivation-score.txt"
      via: "Read threshold integer, compare to captivation total"
      pattern: "expected-captivation-score\\.txt"
---

<objective>
Create the `/book-crafter:sample` skill — a one-command end-to-end demo that runs the full pipeline on the tiny-book fixture and emits a machine-greppable PASS/FAIL line.

Purpose: PKG-09. The sample skill is the single-command smoke-test that both recipients and release.sh (potential future gate) can run to verify the plugin is installed and working. It is the only Phase 11 artefact that actually exercises the full pipeline.
Output: `skills/sample/SKILL.md` with the complete skill specification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-distribution-packaging/11-CONTEXT.md
@.planning/phases/11-distribution-packaging/11-RESEARCH.md
@skills/orchestrator/SKILL.md
@fixtures/tiny-book/brief.md
@fixtures/tiny-book/expected-captivation-score.txt
@scripts/craft-check.js
@references/captivation-rubric.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write skills/sample/SKILL.md — /book-crafter:sample end-to-end demo</name>
  <read_first>
    - .planning/phases/11-distribution-packaging/11-CONTEXT.md (D-09, D-10, D-11, D-12, D-13 — sample skill contract)
    - .planning/phases/11-distribution-packaging/11-RESEARCH.md §"Sample skill SKILL.md (template)" and §"Open Questions" #2, #3
    - skills/orchestrator/SKILL.md (first 100 lines — understand project creation and Mode 6 fresh trigger phrase)
    - skills/voice-builder/SKILL.md or any existing skill (for frontmatter convention)
    - scripts/craft-check.js (understand fallback craft-check invocation)
  </read_first>
  <files>skills/sample/SKILL.md</files>
  <action>
    Create `skills/sample/SKILL.md` following the existing plugin skill frontmatter convention (see `skills/orchestrator/SKILL.md` for the shape — `name`, `description`, `allowed-tools`).

    **Skill identity:**
    - Directory: `skills/sample/`
    - File: `SKILL.md`
    - Invoked as `/book-crafter:sample` (namespace comes from plugin.json name at install time; the release name `book-crafter` is what recipients see per D-22)

    **Frontmatter** (match the pattern in `skills/orchestrator/SKILL.md`):

    ```markdown
    ---
    name: sample
    description: "Run a full Book Crafter pipeline end-to-end on the built-in tiny-book fixture and print a one-line PASS/FAIL summary with captivation score. Use when the user says 'run sample', 'try book crafter', 'demo', 'sample book', 'smoke test', or wants a quick end-to-end verification after install."
    allowed-tools: Read, Write, Bash, Grep, Glob, Agent
    ---
    ```

    **Body sections (required — follow this order exactly):**

    1. **Title + one-line purpose.** Heading `# Book Crafter Sample`. One sentence: "One-command end-to-end smoke-test on the built-in tiny-book fixture."

    2. **§1. Locate the fixture.** The fixture ships inside the plugin directory. Reference it via `${CLAUDE_PLUGIN_ROOT}`:
       - `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/brief.md`
       - `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/expected-captivation-score.txt`
       Both must exist before continuing. If either is missing, emit `SAMPLE FAIL — fixture missing at <path>` and exit 1.

    3. **§2. Detect re-invocation (D-11 — filesystem-as-state).** Check whether `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/run/` exists as a directory.
       - **Absent → first run.** Invoke orchestrator in normal mode. Do NOT include the phrase "start fresh" in the invocation prompt.
       - **Present → re-invocation.** Invoke orchestrator in Mode 6 by INCLUDING the phrase "start fresh" literally in the invocation prompt. Per 11-RESEARCH.md finding #5, Mode 6 is triggered by the phrase, not a CLI flag.
       State this contract explicitly in the skill body so the executing Claude reads the correct branch. Example phrasing in the skill:

       > If `fixtures/tiny-book/run/` already exists, include the phrase **"start fresh"** in the orchestrator invocation so Mode 6 wipes prior artefacts before re-running. If it does not exist, invoke the orchestrator normally.

    4. **§3. Invoke the orchestrator programmatically.** Use the Agent tool to spawn the `book-crafter:orchestrator` skill with these parameters in the invocation prompt:
       - Project path: `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/run/`
       - Brief: contents of `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/brief.md`
       - Voice profile: `spiritual-default`
       - Size tier: `booklet`
       - **Final `.docx` output path: `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/run/final/<name>.docx`** — the sample skill MUST instruct the formatter to write the final `.docx` to `fixtures/tiny-book/run/final/` explicitly. This overrides any orchestrator default output location. Per D-12, the PASS line path is locked to `fixtures/tiny-book/run/final/<name>.docx` verbatim; the sample skill owns this override and the invocation prompt must state it in plain language (e.g., "write the final .docx to ${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/run/final/").
       - Execution mode: **Full Pipeline, no review gates** (the fixture is pre-approved by virtue of existing in the repo — D-09)
       - Include "start fresh" only if re-invocation detected in §2

       Wait for the pipeline to complete (outline → research → write → edit → enrich → format). After completion, confirm the `.docx` landed in `fixtures/tiny-book/run/final/`; if it did not, emit `SAMPLE FAIL — .docx missing at fixtures/tiny-book/run/final/` and exit 1.

    5. **§4. Compute the captivation score.** Read `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/run/reports/consistency-report.md` (editor emits this per Phase 10). Extract the total captivation score in `N/14` format. If the report exists but the total line is missing, fall back to running `craft-check.js` manually:

       ```
       node "${CLAUDE_PLUGIN_ROOT}/scripts/craft-check.js" "${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/run/edited/ch01-final.md"
       ```

       craft-check output is JSON — parse pass/fail but note that craft-check only covers CRAFT-01/02/05/07/15, not the full 14-point captivation rubric. The primary source is the consistency-report total.

    6. **§5. Compare to threshold and emit the PASS/FAIL line (D-12, D-13).**

       Read `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/expected-captivation-score.txt` — a single integer M.

       Locate the final `.docx` at `${CLAUDE_PLUGIN_ROOT}/fixtures/tiny-book/run/final/` (orchestrator's formatter writes here — the exact filename derives from the book title in the brief).

       **PASS condition (both must hold):**
       - `.docx` exists in `fixtures/tiny-book/run/final/`
       - Captivation total `N >= M`

       **PASS output** (exact format — machine-greppable per D-12):
       ```
       SAMPLE PASS — .docx at fixtures/tiny-book/run/final/<name>.docx, captivation N/14 (threshold M)
       ```

       **FAIL output** (exact format):
       ```
       SAMPLE FAIL — <specific reason> (see fixtures/tiny-book/run/reports/consistency-report.md)
       ```

       Specific failure reasons (one per line, pick the first match):
       - `fixture brief missing`
       - `orchestrator did not complete (no .docx produced)`
       - `.docx missing at fixtures/tiny-book/run/final/`
       - `captivation N below threshold M` (where N and M are the real numbers)
       - `consistency-report.md missing`

    7. **§6. Exit code (D-13).** PASS → exit 0. FAIL → exit 1. This allows future release.sh integration (not in Phase 11 scope, but the contract must be in place).

    **Non-negotiables:**
    - NO interactive gates (D-09). The fixture is pre-approved. Any gate that would normally wait for user input must be bypassed via the "Full Pipeline, no review gates" directive in the orchestrator invocation.
    - NO auto-enabling `--fresh` on first run (D-11). First run = normal. Re-run = fresh.
    - NO writing output outside `fixtures/tiny-book/run/` (D-10). The orchestrator's default `~/Documents/Books/` path must be overridden via the explicit project path in §3.
    - Skill identifier in all internal references is `book-crafter:sample` (release name per D-22), not `book-crafter-dev:sample`.
    - Minimum body length: ≥ 60 lines. Sample skill is the user-facing demo; it needs enough detail that a fresh-context Claude can execute it without reading the orchestrator skill first.
  </action>
  <verify>
    <automated>test -f skills/sample/SKILL.md && head -20 skills/sample/SKILL.md | grep -q '^name: sample$' && head -20 skills/sample/SKILL.md | grep -q '^description:' && head -20 skills/sample/SKILL.md | grep -q '^allowed-tools:' && grep -q 'fixtures/tiny-book/brief.md' skills/sample/SKILL.md && grep -q 'fixtures/tiny-book/expected-captivation-score.txt' skills/sample/SKILL.md && grep -q 'fixtures/tiny-book/run/' skills/sample/SKILL.md && grep -q 'spiritual-default' skills/sample/SKILL.md && grep -q 'start fresh' skills/sample/SKILL.md && grep -q 'SAMPLE PASS' skills/sample/SKILL.md && grep -q 'SAMPLE FAIL' skills/sample/SKILL.md && grep -q 'captivation' skills/sample/SKILL.md && grep -q 'book-crafter:orchestrator' skills/sample/SKILL.md && ! grep -q 'book-crafter-dev' skills/sample/SKILL.md && test "$(wc -l < skills/sample/SKILL.md)" -ge 60</automated>
  </verify>
  <acceptance_criteria>
    - `skills/sample/SKILL.md` exists
    - Frontmatter has `name: sample`, `description:`, `allowed-tools:`
    - Body references `fixtures/tiny-book/brief.md` and `fixtures/tiny-book/expected-captivation-score.txt`
    - Body references `fixtures/tiny-book/run/` (output location, re-invocation detection)
    - Body references `spiritual-default` voice profile
    - Body includes the phrase `start fresh` (Mode 6 re-run trigger per research finding #5)
    - Body includes `SAMPLE PASS` and `SAMPLE FAIL` format templates
    - Body references `book-crafter:orchestrator` (the orchestrator skill it delegates to)
    - Body does NOT contain `book-crafter-dev`
    - File is ≥ 60 lines
  </acceptance_criteria>
  <done>Sample skill is shippable — ships in the release zip via the `skills/` whitelist entry in release.sh. Plan 06 runs the skill end-to-end as the final smoke test.</done>
</task>

</tasks>

<verification>
- Frontmatter is a valid SKILL.md
- All D-09..D-13 contract points present in the body
- Mode 6 fresh trigger uses phrase, not flag (research finding #5)
- Dev/release identity discipline preserved
</verification>

<success_criteria>
A fresh Claude Code session that runs `/book-crafter:sample` can: (a) locate the fixture, (b) invoke the orchestrator, (c) read the threshold, (d) emit PASS/FAIL with correct exit code. Plan 06 will prove this end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/11-distribution-packaging/11-05-SUMMARY.md` per the standard summary template.
</output>
