---
phase: 09-wire-sermon-adapter-to-outliner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/outliner/SKILL.md
autonomous: true
requirements:
  - OUTL-06
  - ENH-01
  - ENH-02

must_haves:
  truths:
    - "Outliner prefers sources-adapted/ over sources/ when adapted content exists"
    - "Outliner falls back to sources/ when sources-adapted/ does not exist"
    - "Outliner logs which source directory it is using (adapted vs raw)"
    - "Section 4 Step 1 reads from whichever directory Section 2 resolved"
  artifacts:
    - path: "skills/outliner/SKILL.md"
      provides: "Updated mode detection and source reading with sources-adapted/ preference"
      contains: "sources-adapted/"
  key_links:
    - from: "skills/sermon-adapter/SKILL.md"
      to: "skills/outliner/SKILL.md"
      via: "sources-adapted/ directory convention"
      pattern: "sources-adapted"
    - from: "skills/outliner/SKILL.md Section 2"
      to: "skills/outliner/SKILL.md Section 4 Step 1"
      via: "resolved source directory variable"
      pattern: "source directory identified in Section 2"
---

<objective>
Update the outliner SKILL.md to prefer `sources-adapted/` over `sources/` when adapted content exists, closing the integration gap between the sermon adapter (Stage 0.5) and the outliner (Stage 1).

Purpose: The sermon adapter writes transformed content to `sources-adapted/` but the outliner hardcodes `sources/` in two places, ignoring the adapted content entirely. This is the last broken integration point in the v1.0 pipeline.

Output: Updated `skills/outliner/SKILL.md` with three-step source directory resolution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-wire-sermon-adapter-to-outliner/09-RESEARCH.md

@skills/outliner/SKILL.md
@skills/sermon-adapter/SKILL.md
@skills/orchestrator/SKILL.md

<interfaces>
<!-- The outliner SKILL.md has two hardcoded references to sources/ that must change: -->

From skills/outliner/SKILL.md line 33 (Section 2 - Mode Detection):
```markdown
- If a `sources/` directory exists and contains `.md`, `.txt`, or `.docx` files: use **Source Ingestion Mode** (section 4)
```

From skills/outliner/SKILL.md line 121 (Section 4 Step 1 - Source Reading):
```markdown
Read all files from the `sources/` directory (`.md`, `.txt`, `.docx` files).
```

<!-- The sermon adapter writes to sources-adapted/ with this marker pattern: -->
<!-- SERMON ADAPTED metadata marker in output files -->
<!-- The orchestrator already checks for sources-adapted/ in its state detection -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update outliner SKILL.md — source directory resolution with sources-adapted/ preference</name>
  <files>skills/outliner/SKILL.md</files>
  <read_first>
    - skills/outliner/SKILL.md (the file being modified — read current Section 2 lines 29-38 and Section 4 Step 1 lines 119-121)
    - skills/sermon-adapter/SKILL.md (confirms adapter writes to sources-adapted/ — verify directory convention)
    - .planning/phases/09-wire-sermon-adapter-to-outliner/09-RESEARCH.md (exact replacement text provided in Code Examples section)
  </read_first>
  <action>
Edit `skills/outliner/SKILL.md` in three locations:

**Location 1: Section 2 "Determine Mode" (lines 31-38)**

Replace the current two-bullet mode detection with a three-step priority check. The EXACT replacement text for lines 31-38:

```markdown
Check the project directory for source content, preferring adapted content when available:

1. If a `sources-adapted/` directory exists and contains `.md`, `.txt`, or `.docx` files: use **Source Ingestion Mode** (section 4) reading from `sources-adapted/`
2. Else if a `sources/` directory exists and contains `.md`, `.txt`, or `.docx` files: use **Source Ingestion Mode** (section 4) reading from `sources/`
3. If neither directory contains source files: use **Topic Brief Mode** (section 3)

Log which mode is being used:
- "Mode: Topic Brief" -- generating outline from topic brief and metadata
- "Mode: Source Ingestion (adapted, [N] source files from sources-adapted/)" -- using sermon-adapted content
- "Mode: Source Ingestion ([N] source files from sources/)" -- using raw source content
```

**Location 2: Section 4 Step 1 "Read and analyse all source files" (lines 119-121)**

Replace the current hardcoded `sources/` reference. The EXACT replacement text for lines 119-121:

```markdown
### Step 1: Read and analyse all source files

Read all files from the source directory identified in Section 2 (either `sources-adapted/` or `sources/`). Accept `.md`, `.txt`, or `.docx` files.

When reading from `sources-adapted/`, these files have already been transformed from spoken to written rhythm by the sermon adapter. The outliner should treat them as written prose -- do not apply any additional spoken-to-written transformations.
```

**Location 3: Verify no other hardcoded `sources/` references exist**

Run `grep -n "sources/" skills/outliner/SKILL.md` after editing to confirm all references to `sources/` are either:
- Part of the fallback logic (step 2 of the priority check), or
- In `sources-adapted/` (the preferred path)

There should be zero standalone `sources/` references that ignore `sources-adapted/`.
  </action>
  <verify>
    <automated>grep -c "sources-adapted" skills/outliner/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sources-adapted" skills/outliner/SKILL.md` returns >= 3 (at minimum: Section 2 step 1, Section 2 log line, Section 4 Step 1)
    - `grep -n "sources-adapted" skills/outliner/SKILL.md` shows matches in both Section 2 (around lines 31-39) and Section 4 (around lines 119-125)
    - Section 2 contains the three-step numbered list: "1. If a `sources-adapted/`" ... "2. Else if a `sources/`" ... "3. If neither"
    - Section 2 log messages include "Mode: Source Ingestion (adapted," for the adapted path
    - Section 4 Step 1 contains "source directory identified in Section 2" (not hardcoded to either directory)
    - Section 4 Step 1 contains "do not apply any additional spoken-to-written transformations" guidance
    - No standalone `sources/` reference exists that ignores `sources-adapted/` (every `sources/` mention is paired with a `sources-adapted/` preference or is the fallback)
  </acceptance_criteria>
  <done>Outliner SKILL.md Section 2 uses three-step priority (sources-adapted/ -> sources/ -> Topic Brief), Section 4 Step 1 reads from the resolved directory, and all `sources/` references include the adapted fallback logic</done>
</task>

</tasks>

<verification>
1. `grep -c "sources-adapted" skills/outliner/SKILL.md` returns >= 3
2. `grep -n "sources" skills/outliner/SKILL.md` shows all source directory references are correct (adapted preferred, raw as fallback)
3. Visual review: Section 2 has numbered 1-2-3 priority check, Section 4 Step 1 references "source directory identified in Section 2"
</verification>

<success_criteria>
- The outliner SKILL.md prefers `sources-adapted/` when present, falls back to `sources/`, and uses Topic Brief Mode when neither has content
- The sermon-to-book pipeline is wired end-to-end: sermon adapter (Stage 0.5) writes to `sources-adapted/`, outliner (Stage 1) reads from `sources-adapted/`
- Non-sermon source ingestion path (direct `sources/` usage) continues to work unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-wire-sermon-adapter-to-outliner/09-01-SUMMARY.md`
</output>
