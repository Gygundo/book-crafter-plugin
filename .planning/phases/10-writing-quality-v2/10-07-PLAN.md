---
phase: 10-writing-quality-v2
plan: 07
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - references/voice-profiles/spiritual-default.md
  - references/voice-profile-spec.md
  - skills/voice-builder/SKILL.md
  - .planning/phases/10-writing-quality-v2/10-CONTEXT.md
autonomous: true
requirements: [CRAFT-13]
must_haves:
  truths:
    - "spiritual-default.md is at most 150 lines"
    - "spiritual-default.md has a Reader Moments section with at least 12 concrete moments across at least 3 mood categories"
    - "Every v2 addition is paired with a documented v1 removal in the kill list"
    - "Theological Framework section is byte-identical to Phase 7 (hard constraint)"
    - "voice-profile-spec.md documents Reader Moments as a recommended optional section"
    - "voice-builder skill generates a Reader Moments section in output"
  artifacts:
    - path: "references/voice-profiles/spiritual-default.md"
      provides: "Subtractively audited voice profile under 150 lines with Reader Moments"
      contains: "Reader Moments"
    - path: "references/voice-profile-spec.md"
      provides: "Spec with Reader Moments as recommended optional section"
      contains: "Reader Moments"
    - path: "skills/voice-builder/SKILL.md"
      provides: "Voice builder generates Reader Moments section"
      contains: "Reader Moments"
  key_links:
    - from: "references/voice-profiles/spiritual-default.md"
      to: "references/bestseller-craft-rules.md § CRAFT-06"
      via: "writer selects ≥2 moments per chapter"
      pattern: "Reader Moments"
---

<objective>
Subtractive audit of `references/voice-profiles/spiritual-default.md` (CRAFT-13): add a Reader Moments section (≥12 concrete moments) to enable CRAFT-06, pair every addition with a documented v1 removal, cap the file at 150 lines, preserve the Theological Framework byte-for-byte, and commit the kill list inline in 10-CONTEXT.md. Update voice-profile-spec.md and voice-builder skill accordingly.

Purpose: Prevent the Phase 7 kitchen-sink failure mode. Every new rule or section in the voice profile MUST displace old text, not stack on top.

**This plan runs in PARALLEL with Plans 3, 4, 5, 6** (all wave 3+). It depends only on Plan 2 (for cross-reference consistency with bestseller-craft-rules.md § CRAFT-06). Its kill list must be committed before Phase 10 verification.
Output: Updated spiritual-default.md ≤150 lines, voice-profile-spec.md with Reader Moments documented, voice-builder SKILL.md generating Reader Moments, kill list populated in 10-CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-writing-quality-v2/10-CONTEXT.md
@.planning/phases/10-writing-quality-v2/10-RESEARCH.md
@references/voice-profiles/spiritual-default.md
@references/voice-profile-spec.md
@skills/voice-builder/SKILL.md
@references/bestseller-craft-rules.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subtractive audit of spiritual-default.md with Reader Moments section</name>
  <read_first>
    - references/voice-profiles/spiritual-default.md (full 123 lines)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md Q3 (section line counts, subtraction candidates) and Example 3 (Reader Moments section shape)
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md (D-14, D-17, D-18, kill list section)
    - references/bestseller-craft-rules.md § CRAFT-06
  </read_first>
  <files>
    references/voice-profiles/spiritual-default.md
    .planning/phases/10-writing-quality-v2/10-CONTEXT.md
  </files>
  <action>
    **Hard constraints:**
    - File must end at ≤150 lines total.
    - Theological Framework section (current lines 73-83, 11 lines) is BYTE-PRESERVED. Do not touch any of those 11 lines. Verify with a sha256 before/after.
    - Every new line added to the file must be paired with a documented v1 removal in the kill list inside 10-CONTEXT.md.

    **Step 1 — Baseline hash the Theological Framework:**
    Compute sha256 of the Theological Framework section body (current lines 73-83). Store temporarily. After the audit, recompute from the new file location of those 11 lines and assert identical.

    **Step 2 — Add Reader Moments section** (target budget ~20 lines per 10-RESEARCH Example 3):

    Insert a new section titled `## Reader Moments` (place it after the existing Anti-Patterns section and before the Theological Framework so the Theological Framework keeps its relative position). Structure:

    ```markdown
    ## Reader Moments

    > Concrete everyday scenes the reader has actually lived. Writer selects ≥2 per chapter to anchor abstract claims. Per CRAFT-06.

    ### Anxiety
    - The 2am phone-check after an argument
    - The Sunday-night dread before Monday morning
    - The tight-chest silence in the car outside a hospital
    - The cursor blinking on an unfinished job application

    ### Grief
    - The grocery-aisle grief flash when their favourite cereal appears
    - The first Christmas with an empty chair at the table
    - The moment you reach for the phone to tell them, and remember

    ### Doubt
    - The pew you can't stay seated in
    - The prayer that dies halfway through the sentence
    - The worship song you can't sing the chorus of

    ### Joy
    - The kitchen-dance with kids underfoot
    - The first bite of coffee on a morning that finally feels clear
    ```

    Minimum: 12 reader-moments across ≥3 mood categories (above has 13 across 4, giving a tiny buffer).

    **Step 3 — Subtractive removal** (must offset the ~20 lines added):

    Apply these removals (per 10-RESEARCH Q3 "Subtractive audit targets") — each must be logged in the kill list with before/after text:

    1. **Vocabulary > Avoid:** Consolidate any Avoid-bullets that duplicate Anti-Patterns entries. Target: remove 4-6 duplicates (e.g., "let me explain", "it is important to note" if they appear in both sections).

    2. **Calibration Examples:** The 25 lines of calibration examples (current lines 99-123) overlap with the new `references/bestseller-calibration.md` (Plan 6). Keep ONE compact example in the voice profile (referencing the calibration file for others). Target: remove ~15 lines.

    3. **Emphasis Techniques:** Merge any bullets that overlap Sentence Patterns or Scripture Handling. Target: remove ~2-3 lines.

    **Step 4 — Document kill list in 10-CONTEXT.md:**

    Edit `.planning/phases/10-writing-quality-v2/10-CONTEXT.md` kill_list section to replace the placeholder row with real entries. Format (one row per v2 addition):

    | # | V2 Addition | V1 Removal | Rationale |
    |---|-------------|------------|-----------|
    | 1 | `## Reader Moments` heading + intro (3 lines) | Vocabulary > Avoid duplicate bullets `let me explain`, `it is important to note`, `clearly`, `as we can see` (4 lines) | CRAFT-06 needs concrete reader scenes; dropped Avoid bullets already appear in Anti-Patterns. |
    | 2 | `### Anxiety` + 4 reader-moment bullets (5 lines) | Calibration Example 1 body (5 lines) | Calibration body moves to `references/bestseller-calibration.md` Score Level 6 exemplar. |
    | 3 | `### Grief` + 3 bullets (4 lines) | Calibration Example 2 body (5 lines) | Calibration body moves to `references/bestseller-calibration.md`. |
    | 4 | `### Doubt` + 3 bullets (4 lines) | Calibration Example 3 body + header (5 lines) | Calibration body moves to `references/bestseller-calibration.md`. |
    | 5 | `### Joy` + 2 bullets (3 lines) | Emphasis Techniques bullets duplicating Sentence Patterns (3 lines) | Overlap removed. |

    Net line delta must be ≤ 0. Planner may adjust exact counts during execution — the constraint is total file ≤150 lines AND every added line is paired with a removed line documented in the kill list.

    **Step 5 — Verify Theological Framework byte-identity:**
    Re-compute sha256 of the Theological Framework section body in the new file. Must match the Step 1 baseline hash. If it does not match, revert the audit and try again — Phase 7 lock was violated.

    **Step 6 — Verify line count:**
    `wc -l < references/voice-profiles/spiritual-default.md` must return ≤150. If over cap, subtract more from Vocabulary or Calibration Examples sections (never Theological Framework).
  </action>
  <verify>
    <automated>[ $(wc -l < references/voice-profiles/spiritual-default.md) -le 150 ] && grep -q "## Reader Moments" references/voice-profiles/spiritual-default.md && [ $(grep -cE "^- " references/voice-profiles/spiritual-default.md | head -1) -ge 12 ] && grep -q "Reader Moments" .planning/phases/10-writing-quality-v2/10-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l < references/voice-profiles/spiritual-default.md` returns at most 150
    - `grep -c "^## Reader Moments" references/voice-profiles/spiritual-default.md` returns 1
    - `grep -cE "^- " references/voice-profiles/spiritual-default.md` returns at least 12 (accounts for Reader Moments + other bullets)
    - `grep -c "^### " references/voice-profiles/spiritual-default.md` returns at least 3 (mood categories: Anxiety, Grief, Doubt, Joy — at least 3 of the 4)
    - Theological Framework section body sha256 UNCHANGED from Phase 7 baseline (document the hash in the task summary)
    - 10-CONTEXT.md kill_list section has at least 3 populated rows (not the placeholder `_(populated during Plan 7 execution)_`)
    - `grep -c "^| [0-9] |" .planning/phases/10-writing-quality-v2/10-CONTEXT.md` returns at least 3 (kill list rows)
  </acceptance_criteria>
  <done>
    Voice profile under 150 lines, Reader Moments section present with ≥12 moments, kill list committed, Theological Framework byte-preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update voice-profile-spec.md + voice-builder skill to support Reader Moments</name>
  <read_first>
    - references/voice-profile-spec.md (61 lines)
    - skills/voice-builder/SKILL.md
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md (D-14, D-15, D-16)
  </read_first>
  <files>
    references/voice-profile-spec.md
    skills/voice-builder/SKILL.md
  </files>
  <action>
    1. Update `references/voice-profile-spec.md`:
       - Add a new section "Optional Sections" (or append to an existing Optional/Recommended list) documenting `Reader Moments`. Content:

         **Reader Moments** (recommended, optional for custom profiles):
         A bulleted list of ≥12 concrete everyday scenes grouped by mood (anxiety, grief, doubt, joy, etc.). Writer selects ≥2 per chapter for CRAFT-06 enforcement. When absent from a profile, editor runs CRAFT-06 in flag-only mode (no hard fail). See `references/voice-profiles/spiritual-default.md` § Reader Moments for an example.

    2. Update `skills/voice-builder/SKILL.md`:
       - Add a new step to the generation pipeline that produces a Reader Moments section in the output voice profile. Content:

         During corpus analysis, extract concrete everyday scenes the source material mentions or implies (anxiety moments, grief moments, doubt moments, joy moments). Aim for ≥12 across ≥3 mood categories. Output as a `## Reader Moments` section in the generated voice profile file, following the schema in references/voice-profile-spec.md and the example shape in references/voice-profiles/spiritual-default.md.

         If the source corpus does not yield enough concrete moments (fewer than 8), emit a `## Reader Moments` section with what was found and a comment note: `<!-- reader_moments_partial: only N moments extracted from corpus; editor runs CRAFT-06 in flag-only mode -->`
  </action>
  <verify>
    <automated>grep -q "Reader Moments" references/voice-profile-spec.md && grep -q "Reader Moments" skills/voice-builder/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Reader Moments" references/voice-profile-spec.md` is true
    - `grep -q "recommended\|optional" references/voice-profile-spec.md` is true
    - `grep -q "Reader Moments" skills/voice-builder/SKILL.md` is true
    - `grep -q "flag-only mode\|reader_moments_partial" skills/voice-builder/SKILL.md` is true
  </acceptance_criteria>
  <done>
    Spec documents Reader Moments as recommended-optional. Voice builder generates the section during corpus analysis.
  </done>
</task>

</tasks>

<verification>
- Voice profile ≤150 lines
- Theological Framework sha256 unchanged
- Kill list committed with paired removals for every addition
</verification>

<success_criteria>
Subtractive audit holds — spiritual-default.md has new Reader Moments content without growing past the cap, and every addition is traceable to a removal in the kill list.
</success_criteria>

<output>
After completion, create `.planning/phases/10-writing-quality-v2/10-07-SUMMARY.md`
</output>
