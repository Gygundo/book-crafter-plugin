---
phase: 10-writing-quality-v2
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - skills/editor/SKILL.md
autonomous: true
requirements: [CRAFT-01, CRAFT-03, CRAFT-04, CRAFT-06]
must_haves:
  truths:
    - "Editor Pass 2 enforces scene-first strictness beyond provenance presence"
    - "Editor Pass 2 audits central image presence in opening/middle/closing of every chapter"
    - "Editor Pass 2 verifies vulnerability beat traces to a real source line"
    - "Editor Pass 2 audits reader moment concreteness against voice profile Reader Moments section"
  artifacts:
    - path: "skills/editor/SKILL.md"
      provides: "Editor Pass 2 with scene-first strictness, central image audit, vulnerability beat audit, reader moment audit"
      contains: "Central Image Audit"
  key_links:
    - from: "skills/editor/SKILL.md (§3.3 extended)"
      to: "references/bestseller-craft-rules.md"
      via: "rule reference"
      pattern: "bestseller-craft-rules\\.md"
---

<objective>
Extend editor Pass 2 with scene-first strictness (CRAFT-01 quality beyond provenance presence), central image audit (CRAFT-03), vulnerability beat audit (CRAFT-04), and reader moment audit (CRAFT-06) — all LLM judgment sub-sections per D-04 and D-07.

Purpose: Complement Pass 1's deterministic + regex-adjacent checks with cross-chapter, chapter-level judgment checks that only make sense once the chapter is structurally coherent (after voice normalisation in Pass 1).
Output: Updated editor SKILL.md Pass 2 with new sub-sections 3.7, 3.8, 3.9, and an extended 3.3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-writing-quality-v2/10-CONTEXT.md
@.planning/phases/10-writing-quality-v2/10-RESEARCH.md
@skills/editor/SKILL.md
@references/bestseller-craft-rules.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend editor Pass 2 with CRAFT-01 strictness + CRAFT-03/04/06 audits</name>
  <read_first>
    - skills/editor/SKILL.md Section 3 "Pass 2" (lines 216-296 approximately)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md Q4 (Pass 2 insertion map) and Q8 CRAFT-01 LLM judgment rules
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md (D-04, D-07, D-11, D-12, D-16)
    - references/bestseller-craft-rules.md § CRAFT-01, CRAFT-03, CRAFT-04, CRAFT-06
  </read_first>
  <files>skills/editor/SKILL.md</files>
  <action>
    Insertions are additive to Pass 2 (Section 3). Do not remove or renumber existing 3.1-3.6 sub-sections. Plan 1 already pointed §3.3 at captivation-rubric.md; this plan EXTENDS that §3.3 with scene-first strictness in addition (not replacing the rubric invocation).

    1. Extend §3.3 (Opening Engagement) by appending a new block titled "Scene-First Strictness (CRAFT-01 quality)". Content:

       craft-check.js has already verified the provenance comment exists and resolves (Pass 1 §2.0). In Pass 2, the editor performs LLM judgment on scene quality in the first 150 words of the chapter. Count words (whitespace-split) up to 150, then confirm ALL of:
       1. A proper-noun human OR first-person narrator ("I", "me", "my") is present by word 150.
       2. A time-marker phrase is present (examples: "at 2am", "last Tuesday", "the summer I was fourteen", "when I was eight", "three years ago").
       3. A sensory or physical detail is present — light ("sunlight", "streetlight", "dim", "bright"), sound ("hum", "click", "silence", "rustle"), texture ("cold", "damp", "rough"), smell ("coffee", "rain", "smoke"), or a specifically named concrete object ("chair", "phone", "coffee cup", "window", "door", "car").

       If any of (1)(2)(3) is missing, trigger auto-revise of the chapter opener only (per D-06): request writer rewrite the first paragraph(s) up to 150 words to include the missing element, while preserving the provenance comment and the rest of the chapter.

       Rule reference: ${CLAUDE_PLUGIN_ROOT}/references/bestseller-craft-rules.md § CRAFT-01.

    2. Add new sub-section "3.7 Central Image Audit (CRAFT-03)" at the end of Pass 2:

       Read the chapter's `central_image` field from the outline/Book DNA. Locate three zones in the chapter:
       - Opening 200 words
       - Middle third (word-count-based: from word (total_words/3) to word (2*total_words/3))
       - Closing 200 words

       Confirm the central image (or a semantically equivalent reference) appears in ALL THREE zones. Different registers are fine: literal in opening, metaphor in middle, echo in closing.

       Failure mode: FLAG ONLY (per D-07). Add to diagnostic report with the zones the image was missing from and the line ranges checked. Do not auto-revise — forcing rewrites here causes divergent-improvement failures (see Pitfall 4).

    3. Add sub-section "3.8 Vulnerability Beat Audit (CRAFT-04)" at the end of Pass 2:

       Read the chapter's `vulnerability_beat_seed` field from the outline/Book DNA.
       - If the seed field is empty, skip this audit and note `vulnerability_beat: skipped_no_seed` in the diagnostic report.
       - If the seed field is populated, resolve it (parse source_path:line, read the file, confirm line exists).
       - Locate a first-person vulnerability beat in the chapter's middle third (per Pass 2 §3.7 zone definition).
       - Confirm the beat references or paraphrases the seed material. LLM judgment: does the beat traceably draw on the seed, or does it appear fabricated?

       Failure mode: FLAG ONLY. Flag types:
       - `vulnerability_beat: missing` — no first-person beat in middle third
       - `vulnerability_beat: fabricated` — beat present but does not trace to seed
       - `vulnerability_beat: seed_unresolved` — seed path/line does not exist

       Fabricated beats are the most serious flag but still flag-only, not auto-revise, because auto-revise on judgment checks risks divergent improvement (D-07 rationale and Pitfall 5: forcing a rewrite would loop back to fabrication).

    4. Add sub-section "3.9 Reader Moment Audit (CRAFT-06)" at the end of Pass 2:

       Read the voice profile's Reader Moments section.
       - If voice profile has no Reader Moments section: skip this audit and note `reader_moments: skipped_no_section` in the diagnostic report (D-16).
       - If present: read the chapter's METADATA `reader_moments_used` field. Confirm (a) at least 2 moments claimed, (b) each claimed moment appears in the chapter text, (c) each claimed moment is one from the voice profile's list.

       Failure mode: FLAG ONLY. Add to diagnostic report with count and list of missing/extra moments.

       Rule reference: ${CLAUDE_PLUGIN_ROOT}/references/bestseller-craft-rules.md § CRAFT-06.

    5. Update §3.6 (Pass 2 Output) to document that the VOICE AUDIT block for Pass 2 now additionally records:
       - craft_pass2:
           central_image: pass | flag (zones missing: [...])
           vulnerability_beat: pass | flag (missing | fabricated | seed_unresolved | skipped_no_seed)
           reader_moments: pass | flag (count: N; missing: [...]) | skipped_no_section
           scene_first_strictness: pass | revised

    Do not touch Pass 1, Pass 3, or existing rubric invocations from Plan 1.
  </action>
  <verify>
    <automated>grep -q "Scene-First Strictness" skills/editor/SKILL.md && grep -q "3.7 Central Image Audit" skills/editor/SKILL.md && grep -q "3.8 Vulnerability Beat Audit" skills/editor/SKILL.md && grep -q "3.9 Reader Moment Audit" skills/editor/SKILL.md && grep -q "craft_pass2:" skills/editor/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Scene-First Strictness" skills/editor/SKILL.md` is true
    - `grep -q "3.7 Central Image Audit" skills/editor/SKILL.md` is true
    - `grep -q "3.8 Vulnerability Beat Audit" skills/editor/SKILL.md` is true
    - `grep -q "3.9 Reader Moment Audit" skills/editor/SKILL.md` is true
    - `grep -q "vulnerability_beat_seed" skills/editor/SKILL.md` is true
    - `grep -q "middle third" skills/editor/SKILL.md` is true
    - `grep -q "craft_pass2:" skills/editor/SKILL.md` is true
    - `grep -q "skipped_no_seed\|seed_unresolved" skills/editor/SKILL.md` is true
    - Plan 1 captivation-rubric references still present: `grep -c "captivation-rubric.md" skills/editor/SKILL.md` returns at least 5
  </acceptance_criteria>
  <done>
    Editor Pass 2 performs scene-first strictness audit + central image/vulnerability beat/reader moment judgment audits. All four respect the auto-revise vs flag-only policy.
  </done>
</task>

</tasks>

<verification>
- Both Pass 1 (Plan 4) and Pass 2 (Plan 5) craft sections coexist in editor skill with distinct sub-section numbers
- No regression on rubric invocations from Plan 1 Task 3
</verification>

<success_criteria>
Editor now enforces all seven CRAFT-01..08 rules across Pass 1 (deterministic + quick judgment) and Pass 2 (cross-zone and cross-chapter judgment).
</success_criteria>

<output>
After completion, create `.planning/phases/10-writing-quality-v2/10-05-SUMMARY.md`
</output>
