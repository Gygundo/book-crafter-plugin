---
phase: 10-writing-quality-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/craft-check.js
  - scripts/test-craft-check.js
  - scripts/test-rubric-regression.js
  - fixtures/phase10/known-good/ch01-draft.md
  - fixtures/phase10/known-bad/ch01-pulpit.md
  - fixtures/phase10/known-bad/ch02-greek-overflow.md
  - fixtures/phase10/known-bad/ch03-no-provenance.md
  - fixtures/phase10/known-bad/ch04-no-version-stamp.md
  - fixtures/phase10/known-bad/ch05-no-reader-thought.md
  - fixtures/phase10/baseline-scores.json
  - references/captivation-rubric.md
  - skills/editor/SKILL.md
autonomous: true
requirements: [CRAFT-09]
must_haves:
  truths:
    - "craft-check.js runs on any markdown file and emits JSON for CRAFT-01/02/05/07/15"
    - "Unit fixtures prove pass/fail cases for each deterministic check"
    - "Captivation rubric lives in references/captivation-rubric.md (extracted pure-move)"
    - "Baseline captivation scores on eternally-secure-ch1 fixture are unchanged after extraction"
  artifacts:
    - path: "scripts/craft-check.js"
      provides: "Deterministic CRAFT-01/02/05/07/15 checker, zero-dep"
      min_lines: 80
    - path: "scripts/test-craft-check.js"
      provides: "node:test unit suite for all deterministic checks"
    - path: "scripts/test-rubric-regression.js"
      provides: "Rubric extraction regression harness (CRAFT-09)"
    - path: "fixtures/phase10/baseline-scores.json"
      provides: "Pre-extraction captivation scores for regression lock"
    - path: "references/captivation-rubric.md"
      provides: "Extracted 5-component rubric (pure move from editor)"
      contains: "Pacing Variety"
  key_links:
    - from: "scripts/test-craft-check.js"
      to: "scripts/craft-check.js"
      via: "execSync invocation of node craft-check.js"
      pattern: "craft-check.js"
    - from: "skills/editor/SKILL.md"
      to: "references/captivation-rubric.md"
      via: "section invocations replacing inline rubric text"
      pattern: "captivation-rubric\\.md"
---

<objective>
Establish Phase 10's validation infrastructure (Wave 0) AND extract the captivation rubric into a standalone reference file with regression lock (CRAFT-09).

Purpose: Every downstream plan depends on (a) a working deterministic checker with fixtures the test-suite can run on every commit, and (b) a pure-move rubric extraction that downstream Plan 6 can safely extend. No rule-addition plan can run until these two foundations exist.
Output: `scripts/craft-check.js` + test harness + 6 fixtures + baseline-scores.json + `references/captivation-rubric.md` + editor skill edits that delegate rubric measurement to the new reference file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-writing-quality-v2/10-CONTEXT.md
@.planning/phases/10-writing-quality-v2/10-RESEARCH.md
@.planning/phases/10-writing-quality-v2/10-VALIDATION.md
@skills/editor/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — craft-check.js + fixtures + unit tests</name>
  <read_first>
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md (§Q8, Example 1 craft-check.js skeleton, Example 2 test harness — COPY verbatim)
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md (D-03, D-19, D-20, D-22)
  </read_first>
  <files>
    scripts/craft-check.js
    scripts/test-craft-check.js
    fixtures/phase10/known-good/ch01-draft.md
    fixtures/phase10/known-bad/ch01-pulpit.md
    fixtures/phase10/known-bad/ch02-greek-overflow.md
    fixtures/phase10/known-bad/ch03-no-provenance.md
    fixtures/phase10/known-bad/ch04-no-version-stamp.md
    fixtures/phase10/known-bad/ch05-no-reader-thought.md
  </files>
  <behavior>
    - CRAFT-01: `known-good/ch01-draft.md` passes; `known-bad/ch03-no-provenance.md` fails with exit code 1
    - CRAFT-02: `known-bad/ch02-greek-overflow.md` (≥4 distinct transliterated terms) fails; known-good passes
    - CRAFT-05: `known-bad/ch01-pulpit.md` (paragraph starting "So let us") fails; known-good with mid-paragraph "So" passes
    - CRAFT-07: `known-bad/ch05-no-reader-thought.md` (<2 italic reader-thought lines) fails
    - CRAFT-15: `known-bad/ch04-no-version-stamp.md` fails
    - JSON output shape: `{chapter_id, checks: {CRAFT-01: {pass, evidence, citations}, CRAFT-02: {...}, CRAFT-05: {...}, CRAFT-07: {...}, CRAFT-15: {...}}}`
    - Exit code 0 when all checks pass; exit code 1 when any check fails; exit code 2 on usage error
  </behavior>
  <action>
    Create `scripts/craft-check.js` COPYING the skeleton from 10-RESEARCH.md §"Example 1: craft-check.js skeleton (pure node stdlib)". Requirements (from D-03, research Q8):

    1. Pure node stdlib only — `require('node:fs')`, `require('node:path')`. NO external deps. NO `require()` of non-stdlib modules.
    2. Constants from research Q8 (copy verbatim):
       - `TRANSLITERATED_TERMS` = ['charis','agape','phileo','eros','storge','dunamis','exousia','logos','rhema','pneuma','sarx','kairos','chronos','sunergeo','pas','shalom','hesed','chesed','ruach','yada','ahavah','nephesh','echad','koinonia','metanoia']
       - `PULPIT_SEAM_REGEX` = `/^(So|Now|And so|Let us|Let me|Here'?s where|Here'?s the thing|You see|Listen|Church|Friend)[\s,.!?]/i`
       - `PROVENANCE_REGEX` (first-line match): `/^<!-- provenance: (.+):(\d+) -->$/`
       - `VERSION_STAMP_REGEX` = `/^<!-- generated-by: book-crafter v\d+\.\d+\.\d+ -->$/m` (must appear within first 3 lines)
       - `READER_THOUGHT_REGEX` = `/(?:^>\s*\*"|^\*")[^"*]{5,200}\?["*]/gm`
    3. `checkCraft01(content, chapterPath)`: first line must match provenance regex; resolve path relative to project root (two dirs up from chapterPath) via `path.resolve`; `fs.existsSync(resolved)` + line-count bounds check. Return `{pass, evidence, citations: ['line 1']}`.
    4. `checkCraft02(content)`: build dynamic regex from TRANSLITERATED_TERMS joined with `|`, case-insensitive word-boundary. `new Set` of lowercased matches — fail when `distinct.size > 3`.
    5. `checkCraft05(content)`: split on `/\n\n+/`, skip paragraphs starting with `#` or `<!--`, test `PULPIT_SEAM_REGEX` on trimmed first word. Return hits with paragraph index and first two words.
    6. `checkCraft07(content)`: `matchAll(READER_THOUGHT_REGEX)`, fail when `< 2`.
    7. `checkCraft15(content)`: test VERSION_STAMP_REGEX; pass/fail.
    8. `main()` reads `process.argv[2]`, reads file, emits `JSON.stringify(result, null, 2)`, exit 1 if any check fails, exit 2 on usage error.
    9. First line of the file: `#!/usr/bin/env node`

    Create `scripts/test-craft-check.js` COPYING Example 2 from research (§"Example 2: node:test fixture harness"). Requirements:
    - `require('node:test')`, `require('node:assert/strict')`, `require('node:child_process').execSync`
    - CHECKER path = `path.join(__dirname, 'craft-check.js')`; FIXTURES = `path.join(__dirname, '..', 'fixtures', 'phase10')`
    - `runChecker(fixturePath)` wraps execSync with try/catch, parses JSON from stdout on error too
    - Tests (at minimum — one per research Example 2):
      - `test('CRAFT-01: known-good chapter has valid provenance', ...)`
      - `test('CRAFT-01: missing provenance fails', ...)` — also asserts `exitCode === 1`
      - `test('CRAFT-02: 4 distinct Greek terms fails cap of 3', ...)`
      - `test('CRAFT-05: "So let us..." at paragraph start fails', ...)`
      - `test('CRAFT-05: mid-paragraph "So" does not trigger', ...)`
      - `test('CRAFT-07: <2 reader-thought lines fails', ...)`
      - `test('CRAFT-15: missing version stamp fails', ...)`

    Create fixtures under `fixtures/phase10/`:
    - `known-good/ch01-draft.md` — starts with `<!-- provenance: fixtures/phase10/known-good/ch01-draft.md:1 -->` (self-reference is fine), `<!-- generated-by: book-crafter v1.1.0 -->` on line 2, `# Chapter 1` heading. Must contain: exactly 2 transliterated terms (e.g. `charis`, `dunamis`) each in separate paragraphs; no pulpit-seam at any paragraph start; 2+ italicised reader-thought lines (e.g. `*"But what if grace isn't enough?"*` and `*"If this is true, why do I still feel empty?"*`); one mid-paragraph "So" (embedded) to prove false-positive safety. Keep it short (~40 lines).
    - `known-bad/ch01-pulpit.md` — valid provenance + version stamp + 2 reader-thought lines, BUT a paragraph starting `So let us consider...`. Must only fail CRAFT-05.
    - `known-bad/ch02-greek-overflow.md` — valid provenance/stamp/reader-thoughts/no-pulpit, BUT contains ≥4 distinct terms from TRANSLITERATED_TERMS (e.g. `charis`, `agape`, `dunamis`, `exousia`). Must only fail CRAFT-02.
    - `known-bad/ch03-no-provenance.md` — starts with `# Chapter 3` (no provenance comment). Must fail CRAFT-01.
    - `known-bad/ch04-no-version-stamp.md` — has provenance line 1, but no `<!-- generated-by:` comment in first 3 lines. Must fail CRAFT-15.
    - `known-bad/ch05-no-reader-thought.md` — valid provenance/stamp/≤3-terms/no-pulpit, BUT zero italic reader-thought lines. Must fail CRAFT-07.

    Each known-bad fixture MUST be isolated — i.e. it should fail ONLY its named check and pass all others. The test harness asserts this.

    Make `scripts/craft-check.js` executable: `chmod +x scripts/craft-check.js`.
  </action>
  <verify>
    <automated>node --test scripts/test-craft-check.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/craft-check.js && test -x scripts/craft-check.js` → true
    - `head -1 scripts/craft-check.js` → `#!/usr/bin/env node`
    - `grep -c "require('node:" scripts/craft-check.js` → ≥ 2
    - `grep -c "require('[^n]" scripts/craft-check.js` → 0 (zero non-stdlib requires)
    - `test -f scripts/test-craft-check.js` → true
    - `ls fixtures/phase10/known-good/ch01-draft.md fixtures/phase10/known-bad/ch01-pulpit.md fixtures/phase10/known-bad/ch02-greek-overflow.md fixtures/phase10/known-bad/ch03-no-provenance.md fixtures/phase10/known-bad/ch04-no-version-stamp.md fixtures/phase10/known-bad/ch05-no-reader-thought.md` → all 6 exist
    - `node --test scripts/test-craft-check.js` → exit 0, all tests pass
    - `node scripts/craft-check.js fixtures/phase10/known-good/ch01-draft.md` → exit 0, JSON with every check `pass: true`
    - `node scripts/craft-check.js fixtures/phase10/known-bad/ch02-greek-overflow.md; echo $?` → `1`, JSON shows CRAFT-02 `pass: false`, all other checks `pass: true`
  </acceptance_criteria>
  <done>
    craft-check.js runs deterministically on all 6 fixtures, test harness green, zero external deps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture baseline captivation scores (pre-extraction regression lock)</name>
  <read_first>
    - skills/editor/SKILL.md lines 66-411 (Pass 1, Pass 2, Pass 3 — rubric source locations per 10-RESEARCH §Q1)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md §Q9 (Validation architecture, baseline JSON shape)
  </read_first>
  <files>
    fixtures/phase10/baseline-scores.json
    scripts/test-rubric-regression.js
  </files>
  <action>
    Baseline-score capture is done WITHOUT running the live editor (LLM nondeterminism would drift the baseline). Instead:

    1. Read current `skills/editor/SKILL.md` and extract the 5 captivation component scoring rules verbatim from sections 2.4 (Pacing Variety), 2.5 (Emotional Connection), 2.5.5 (Reader Engagement), 3.3 (Opening Engagement), 3.4 (Chapter-Ending Momentum). Record the EXACT line ranges you found them in inside `baseline-scores.json` as the `source_locations` field — this is the regression anchor (Task 3 must preserve identical scoring logic after move).

    2. Create `fixtures/phase10/baseline-scores.json` with:
    ```json
    {
      "version": "pre-extraction",
      "captured_at": "2026-04-15",
      "rubric_components": [
        { "id": "pacing_variety", "max": 2, "source_location": "skills/editor/SKILL.md §2.4 lines N-M" },
        { "id": "emotional_connection", "max": 2, "source_location": "skills/editor/SKILL.md §2.5 lines N-M" },
        { "id": "reader_engagement", "max": 2, "source_location": "skills/editor/SKILL.md §2.5.5 lines N-M" },
        { "id": "opening_engagement", "max": 2, "source_location": "skills/editor/SKILL.md §3.3 lines N-M" },
        { "id": "ending_momentum", "max": 2, "source_location": "skills/editor/SKILL.md §3.4 lines N-M" }
      ],
      "total_max": 10,
      "scoring_logic_hash": "<sha256 of concatenated §2.4+§2.5+§2.5.5+§3.3+§3.4 body text>",
      "regression_rule": "After CRAFT-09 extraction, concatenating the equivalent sections of references/captivation-rubric.md MUST yield an identical sha256 OR the move is impure and Plan 1 Task 4 must be reworked."
    }
    ```

    Compute the sha256 with `node -e` using `crypto.createHash('sha256').update(text).digest('hex')`, where `text` is the concatenation of the five section bodies (headings excluded, trimmed, normalised to LF). Store the exact hash in the JSON.

    3. Create `scripts/test-rubric-regression.js`:
    ```
    #!/usr/bin/env node
    // Regression test for CRAFT-09 pure-move extraction.
    // Reads baseline-scores.json, reads references/captivation-rubric.md (after Task 4),
    // re-computes sha256 over the extracted rubric body, compares to baseline.scoring_logic_hash.
    // Exit 0 if identical. Exit 1 if drifted.
    ```
    The script uses `node:test` or plain assert + process.exit. It MUST fail loud when references/captivation-rubric.md does not yet exist (Task 4 output) — that's fine at this point; the test becomes green after Task 4.

    The test reads the rubric file, extracts the component blocks by locating headings matching `/^###? (Pacing Variety|Emotional Connection|Reader Engagement|Opening Engagement|Chapter-Ending Momentum)/`, concatenates bodies in the same order as baseline, normalises, hashes, compares.
  </action>
  <verify>
    <automated>test -f fixtures/phase10/baseline-scores.json && node -e "const b=require('./fixtures/phase10/baseline-scores.json'); if(!b.scoring_logic_hash||b.rubric_components.length!==5) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f fixtures/phase10/baseline-scores.json` → true
    - `node -e "const b=require('./fixtures/phase10/baseline-scores.json'); console.log(b.rubric_components.length)"` → `5`
    - `node -e "const b=require('./fixtures/phase10/baseline-scores.json'); console.log(b.scoring_logic_hash.length)"` → `64`
    - `test -f scripts/test-rubric-regression.js` → true
    - `grep -q "scoring_logic_hash" scripts/test-rubric-regression.js` → true
  </acceptance_criteria>
  <done>
    Baseline hash captured. Regression harness in place, will assert identity after Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract captivation rubric (pure move) + update editor skill</name>
  <read_first>
    - skills/editor/SKILL.md §2.4, §2.5, §2.5.5, §3.3, §3.4, §4.5 (the exact ranges logged in baseline-scores.json)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md §Q1 (extract plan), §"Pattern 3: Pure Move Extraction", §"Pitfall 1: Rubric Extraction Introduces Silent Score Drift"
    - fixtures/phase10/baseline-scores.json (for sha256 target)
  </read_first>
  <files>
    references/captivation-rubric.md
    skills/editor/SKILL.md
  </files>
  <action>
    This is a PURE MOVE. No rewording. No tightening. No merging bullets. Any edit beyond copy-paste breaks CRAFT-09's regression lock (Pitfall 1).

    1. Create `references/captivation-rubric.md` with this exact top-level structure:
    ```markdown
    # Captivation Rubric

    > Captivation scoring for chapter-level quality. 5 components × 0-2 points = 0-10 total. Invoked by editor Pass 1 (§2.4, §2.5, §2.5.5) and Pass 2 (§3.3, §3.4) — scoring logic lives here as the single source of truth.

    ## Components

    ### Pacing Variety
    <paste §2.4 body verbatim — detection approach + scoring table>

    ### Emotional Connection
    <paste §2.5 body verbatim>

    ### Reader Engagement
    <paste §2.5.5 body verbatim>

    ### Opening Engagement
    <paste §3.3 body verbatim>

    ### Chapter-Ending Momentum
    <paste §3.4 body verbatim>

    ## Scoring Aggregation

    <paste §4.5 component table + threshold table + momentum-aware threshold — verbatim>
    ```

    Component body content is COPIED WORD FOR WORD from the line ranges recorded in `baseline-scores.json`. Heading levels are normalised to `###` (component) and `##` (section); nothing else changes.

    2. Update `skills/editor/SKILL.md`:
    - In §2.4, replace the detailed scoring body with: `Apply the Pacing Variety check as defined in \`${CLAUDE_PLUGIN_ROOT}/references/captivation-rubric.md\` (Components → Pacing Variety).` Keep section heading and any wrapping invocation context.
    - In §2.5, replace with: `Apply the Emotional Connection check as defined in \`${CLAUDE_PLUGIN_ROOT}/references/captivation-rubric.md\` (Components → Emotional Connection).`
    - In §2.5.5, replace with: `Apply the Reader Engagement check as defined in \`${CLAUDE_PLUGIN_ROOT}/references/captivation-rubric.md\` (Components → Reader Engagement).`
    - In §3.3, replace with: `Apply the Opening Engagement check as defined in \`${CLAUDE_PLUGIN_ROOT}/references/captivation-rubric.md\` (Components → Opening Engagement).` Pass 2 will further extend §3.3 in Plan 5 (scene-first strictness) — do not anticipate that here.
    - In §3.4, replace with: `Apply the Chapter-Ending Momentum check as defined in \`${CLAUDE_PLUGIN_ROOT}/references/captivation-rubric.md\` (Components → Chapter-Ending Momentum).`
    - In §4.5, replace the component + threshold tables with: `Scoring aggregation and thresholds per \`${CLAUDE_PLUGIN_ROOT}/references/captivation-rubric.md\` § Scoring Aggregation.` Keep the surrounding consistency-report template text untouched.

    3. Run the regression harness: `node scripts/test-rubric-regression.js`. It must pass, meaning the sha256 of the extracted bodies matches baseline-scores.json.scoring_logic_hash. If it fails, you edited during the move — revert and copy again strictly.
  </action>
  <verify>
    <automated>node scripts/test-rubric-regression.js && grep -q "captivation-rubric.md" skills/editor/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f references/captivation-rubric.md` → true
    - `grep -c "^### Pacing Variety" references/captivation-rubric.md` → 1
    - `grep -c "^### Emotional Connection" references/captivation-rubric.md` → 1
    - `grep -c "^### Reader Engagement" references/captivation-rubric.md` → 1
    - `grep -c "^### Opening Engagement" references/captivation-rubric.md` → 1
    - `grep -c "^### Chapter-Ending Momentum" references/captivation-rubric.md` → 1
    - `grep -c "captivation-rubric.md" skills/editor/SKILL.md` → ≥ 5
    - `node scripts/test-rubric-regression.js` → exit 0 (sha256 identical to baseline)
  </acceptance_criteria>
  <done>
    Captivation rubric lives in one file. Editor delegates to it. Regression lock intact.
  </done>
</task>

</tasks>

<verification>
- Wave 0 infrastructure exists and all unit tests green: `node --test scripts/test-craft-check.js`
- Baseline JSON captured with sha256 regression hash: `test -f fixtures/phase10/baseline-scores.json`
- Rubric extracted pure-move: `node scripts/test-rubric-regression.js` green
- Editor skill delegates all 5 components to captivation-rubric.md
</verification>

<success_criteria>
All three tasks pass their automated verify commands. Downstream plans can now (a) run the deterministic checker, (b) add craft rules without breaking rubric regression, (c) extend the rubric in Plan 6 without breaking baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/10-writing-quality-v2/10-01-SUMMARY.md`
</output>
