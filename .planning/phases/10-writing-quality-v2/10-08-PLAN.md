---
phase: 10-writing-quality-v2
plan: 08
type: execute
wave: 6
depends_on: ["10-02", "10-03", "10-04", "10-05"]
files_modified:
  - skills/orchestrator/SKILL.md
  - skills/writer/SKILL.md
  - skills/editor/SKILL.md
  - skills/researcher/SKILL.md
  - skills/outliner/SKILL.md
  - skills/enricher/SKILL.md
  - skills/formatter/SKILL.md
autonomous: true
requirements: [CRAFT-14, CRAFT-15]
must_haves:
  truths:
    - "Orchestrator has a Mode 6 Fresh Run that deletes locked dirs/files and preserves sources, sources-adapted, brief.md, voice-profile.md"
    - "Fresh mode confirms deletion list with the user before acting"
    - "Every generated artefact carries a version stamp HTML comment on one of the first 3 lines"
    - "Formatter strips HTML comments before .docx emission"
  artifacts:
    - path: "skills/orchestrator/SKILL.md"
      provides: "Mode 6 Fresh Run with locked delete/preserve lists + user confirmation"
      contains: "Fresh Run"
    - path: "skills/formatter/SKILL.md"
      provides: "Formatter strips HTML comments before .docx emission"
      contains: "HTML comment"
  key_links:
    - from: "skills/orchestrator/SKILL.md"
      to: "CRAFT-14 delete/preserve lists"
      via: "Mode 6 preprocessing step"
      pattern: "Fresh Run"
    - from: "skills/formatter/SKILL.md"
      to: "HTML comment stripping"
      via: "pre-docx text transform"
      pattern: "generated-by"
---

<objective>
Add Mode 6 Fresh Run to the orchestrator (CRAFT-14) with locked delete/preserve lists and mandatory user confirmation, AND wire version stamps (CRAFT-15) into every skill that emits an artefact plus formatter HTML-comment stripping at .docx emission.

Purpose: Fresh re-runs are the prerequisite for Phase 12's seven-gap comparison; version stamps are the regression-chain anchor. Both must ship together so Phase 12 can invoke `--fresh` and expect every artefact to carry an identifying stamp.
Output: Updated orchestrator, six skill files stamping their outputs, formatter stripping comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-writing-quality-v2/10-CONTEXT.md
@.planning/phases/10-writing-quality-v2/10-RESEARCH.md
@skills/orchestrator/SKILL.md
@skills/writer/SKILL.md
@skills/editor/SKILL.md
@skills/researcher/SKILL.md
@skills/outliner/SKILL.md
@skills/formatter/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mode 6 Fresh Run to orchestrator</name>
  <read_first>
    - skills/orchestrator/SKILL.md (Section 3 state detection lines 156-222, Section 5 next-stage identification, Section 6 Execution Modes)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md Q6 (fresh-mode wiring and delete/preserve lists)
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md Pitfall 10 (silent delete)
    - .planning/REQUIREMENTS.md CRAFT-14 (authoritative delete/preserve lists)
  </read_first>
  <files>skills/orchestrator/SKILL.md</files>
  <action>
    1. Add "Mode 6: Fresh Run" to Section 6 Execution Modes (alongside existing Guided/Full/Resume/Status/Revision modes). Content:

       **Mode 6: Fresh Run**

       Trigger phrases (natural-language detection): "start fresh", "rerun from scratch", "fresh build", "regenerate everything", "--fresh". If any appears in the user utterance during invocation, the orchestrator enters Mode 6 before running state detection (Section 3).

       Mode 6 preprocessing steps:
       1. Identify the project directory from the user's request.
       2. Compute the delete list and preserve list against the project directory:
          - Delete list: `book-dna.md`, `chapter-outline.md`, `research/`, `drafts/`, `edited/`, `revisions/`, `enrichments/`, `front-matter/`, `reports/`, `output/`
          - Preserve list: `sources/`, `sources-adapted/`, `brief.md`, `voice-profile.md`
       3. **Mandatory confirmation prompt** — never silent delete:
          > "Fresh mode will delete the following in `{project_path}`: book-dna.md, chapter-outline.md, research/, drafts/, edited/, revisions/, enrichments/, front-matter/, reports/, output/. Preserved: sources/, sources-adapted/, brief.md, voice-profile.md. Proceed? (yes/no)"
          If user responds anything other than an affirmative ("yes", "y", "proceed", "confirm"), abort Mode 6 and fall through to Resume mode.
       4. On affirmative, perform the deletes (idempotent — items that don't exist are skipped silently; non-empty dirs are rm-rf'd).
       5. Re-enter state detection (Section 3). Because the delete list covers every downstream artefact, state detection will identify the earliest pipeline stage as the next stage.

    2. Add new Section 5.0 "Fresh Mode Preprocessing" note at the top of Section 5 "Identify the Next Stage", stating that when Mode 6 is active, the fresh preprocessing runs BEFORE normal state detection (per step 5 above).

    3. Add a subsection at the end of Section 6 Mode 6 titled "Safety Invariants":
       - Never delete `sources/`, `sources-adapted/`, `brief.md`, or `voice-profile.md`.
       - Never delete without explicit user confirmation.
       - Never re-prompt without showing the specific paths about to be deleted.
  </action>
  <verify>
    <automated>grep -q "Mode 6: Fresh Run" skills/orchestrator/SKILL.md && grep -F "Proceed? (yes/no)" skills/orchestrator/SKILL.md && grep -q "Safety Invariants" skills/orchestrator/SKILL.md && grep -q "voice-profile.md" skills/orchestrator/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Mode 6: Fresh Run" skills/orchestrator/SKILL.md` is true
    - `grep -c "start fresh\|rerun from scratch\|--fresh" skills/orchestrator/SKILL.md` returns at least 3
    - `grep -F "Proceed? (yes/no)" skills/orchestrator/SKILL.md` is true
    - `grep -q "book-dna.md" skills/orchestrator/SKILL.md` is true
    - `grep -q "sources-adapted" skills/orchestrator/SKILL.md` is true
    - `grep -q "Safety Invariants" skills/orchestrator/SKILL.md` is true
  </acceptance_criteria>
  <done>
    Mode 6 Fresh Run is defined, confirmation-gated, with locked delete/preserve lists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire version stamps into every emitting skill + formatter comment stripping</name>
  <read_first>
    - skills/writer/SKILL.md, skills/editor/SKILL.md, skills/researcher/SKILL.md, skills/outliner/SKILL.md, skills/enricher/SKILL.md (current artefact emission sections per 10-RESEARCH Q5 table)
    - skills/formatter/SKILL.md (current .docx conversion path — where text cleanup happens)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md Q5 "Version stamp insertion strategy"
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md D-21 (formatter strips HTML comments)
  </read_first>
  <files>
    skills/writer/SKILL.md
    skills/editor/SKILL.md
    skills/researcher/SKILL.md
    skills/outliner/SKILL.md
    skills/enricher/SKILL.md
    skills/formatter/SKILL.md
  </files>
  <action>
    **Version stamp format (LOCKED):**
    ```
    <!-- generated-by: book-crafter v1.1.0 -->
    ```
    Must appear within the first 3 lines of every generated artefact.

    **Note on dependency ordering:** Plan 10-08 now hard-depends on Plan 10-03 (`depends_on: ["10-02","10-03"]`, wave 4). When this plan executes, Plan 10-03's edits to `skills/writer/SKILL.md` (scene-first/provenance instructions) are already present. This plan ADDS the version-stamp instruction to writer without any conditional fallback branching.

    **Emitting skills — add stamp instructions at artefact emission sites:**

    1. `skills/outliner/SKILL.md`: In the section that writes `chapter-outline.md` and `book-dna.md`, add a line: "Prepend `<!-- generated-by: book-crafter v1.1.0 -->` as the first line of both artefacts."

    2. `skills/researcher/SKILL.md`: In the per-chapter research artefact emission section, add: "Prepend `<!-- generated-by: book-crafter v1.1.0 -->` as the first line of each `research/ch[NN]-research.md` file."

    3. `skills/writer/SKILL.md`: Plan 10-03 has already added the provenance comment as line 1 of every chapter draft. ADD the version stamp as line 2, immediately after the provenance comment and before the chapter heading. Edit the writer's draft-emission instructions to read: "The first two lines of every `drafts/ch[NN]-draft.md` are, in this exact order:
           <!-- provenance: {source_path}:{line} -->
           <!-- generated-by: book-crafter v1.1.0 -->
       followed by the chapter heading." Do NOT remove or duplicate the provenance instruction Plan 10-03 added — only add the version stamp line beneath it.

    4. `skills/editor/SKILL.md`: In each of §2.8 (Pass 1 Output), §3.6 (Pass 2 Output), §4.5 (Pass 3 Output / consistency-report.md), add: "Prepend `<!-- generated-by: book-crafter v1.1.0 -->` as the first line of the emitted file (or the second line if a provenance comment already occupies line 1). For consistency-report.md and flow-report.md the stamp is line 1."

    5. `skills/enricher/SKILL.md`: In the sections that write `enrichments/ch[NN]-enrichments.md` and `front-matter/foreword.md`, add: "Prepend `<!-- generated-by: book-crafter v1.1.0 -->` as the first line."

    **Formatter — strip HTML comments before .docx:**

    6. `skills/formatter/SKILL.md`: In the section that reads chapter markdown and converts to .docx, add a pre-processing step titled "Strip HTML Comments":

       Before passing chapter markdown to docx-js, run a text transform that removes every HTML comment line matching `<!--.*-->`. Use the regex `/<!--[\s\S]*?-->/g` and `.replace()` in the Node script invocation. This covers both provenance comments (`<!-- provenance: ... -->`) and version stamps (`<!-- generated-by: ... -->`). Verify by asserting the final .docx contains zero occurrences of the literal string `generated-by` or `provenance:` in any text run.
  </action>
  <verify>
    <automated>grep -q "generated-by: book-crafter" skills/outliner/SKILL.md && grep -q "generated-by: book-crafter" skills/researcher/SKILL.md && grep -q "generated-by: book-crafter" skills/writer/SKILL.md && grep -q "generated-by: book-crafter" skills/editor/SKILL.md && grep -q "generated-by: book-crafter" skills/enricher/SKILL.md && grep -q "Strip HTML Comments\|strip.*HTML comment" skills/formatter/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "generated-by: book-crafter v1.1.0" skills/outliner/SKILL.md` returns at least 1
    - `grep -c "generated-by: book-crafter v1.1.0" skills/researcher/SKILL.md` returns at least 1
    - `grep -c "generated-by: book-crafter v1.1.0" skills/writer/SKILL.md` returns at least 1
    - `grep -c "generated-by: book-crafter v1.1.0" skills/editor/SKILL.md` returns at least 1
    - `grep -c "generated-by: book-crafter v1.1.0" skills/enricher/SKILL.md` returns at least 1
    - `grep -q "Strip HTML Comments\|strip.*HTML comment\|<!--.*-->" skills/formatter/SKILL.md` is true
    - `grep -q "generated-by\|provenance" skills/formatter/SKILL.md` is true
    - Plan 10-03's provenance instruction in writer is preserved: `grep -q "provenance:" skills/writer/SKILL.md` is true
  </acceptance_criteria>
  <done>
    Every emitting skill stamps its output. Formatter strips comments before .docx emission. Fresh mode + stamps ship together.
  </done>
</task>

</tasks>

<verification>
- Mode 6 is documented with confirmation prompt and safety invariants
- All 5 emitting skills carry version stamp instructions
- Formatter documents HTML comment stripping
</verification>

<success_criteria>
Phase 12 can invoke Mode 6 Fresh Run and expect every artefact to carry a stamp that Phase 12's comparison tooling can use as a regression anchor.
</success_criteria>

<output>
After completion, create `.planning/phases/10-writing-quality-v2/10-08-SUMMARY.md`
</output>
