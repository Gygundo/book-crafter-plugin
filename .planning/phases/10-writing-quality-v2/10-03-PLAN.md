---
phase: 10-writing-quality-v2
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - skills/writer/SKILL.md
  - skills/outliner/SKILL.md
  - references/book-dna-template.md
autonomous: true
requirements: [CRAFT-01, CRAFT-02, CRAFT-03, CRAFT-04, CRAFT-05, CRAFT-06]
must_haves:
  truths:
    - "Writer emits provenance comment as chapter line 1"
    - "Writer threads central_image through opening/middle/closing of every chapter"
    - "Writer places no more than 3 transliterated terms per chapter with at least 3 unpacking sentences each"
    - "Writer selects at least 2 Reader Moments per chapter from voice profile"
    - "Writer places one sourced vulnerability beat in middle third per chapter"
    - "Outliner assigns central_image and vulnerability_beat_seed per chapter, distinct across chapters"
  artifacts:
    - path: "skills/writer/SKILL.md"
      provides: "Writer skill with scene-first, Greek cap, central image, vulnerability beat, reader moments, provenance, pulpit-seam avoidance"
      contains: "provenance"
    - path: "skills/outliner/SKILL.md"
      provides: "Outliner assigns per-chapter central_image and vulnerability_beat_seed"
      contains: "central_image"
    - path: "references/book-dna-template.md"
      provides: "Book DNA template carrying central_image and vulnerability_beat_seed fields"
      contains: "central_image"
  key_links:
    - from: "skills/writer/SKILL.md"
      to: "references/bestseller-craft-rules.md"
      via: "runtime reference read"
      pattern: "bestseller-craft-rules\\.md"
    - from: "skills/outliner/SKILL.md"
      to: "references/book-dna-template.md"
      via: "outliner emits fields that match template"
      pattern: "central_image"
---

<objective>
Update the writer skill with procedural scene-first, Greek cap, central-image, vulnerability-beat, reader-moments, and pulpit-seam-avoidance rules (CRAFT-01/02/03/04/05/06), AND update the outliner to assign central_image and vulnerability_beat_seed per chapter (D-10..D-13).

Purpose: Writer output must pass craft-check.js and editor LLM checks added in later plans. Outliner provides the data the writer needs — without outliner changes, the two new fields would be fabricated.
Output: Updated writer SKILL.md, outliner SKILL.md, and book-dna-template.md carrying the two new fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-writing-quality-v2/10-CONTEXT.md
@.planning/phases/10-writing-quality-v2/10-RESEARCH.md
@skills/writer/SKILL.md
@skills/outliner/SKILL.md
@references/book-dna-template.md
@references/bestseller-craft-rules.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update outliner and book-dna-template with central_image and vulnerability_beat_seed</name>
  <read_first>
    - skills/outliner/SKILL.md (current Chapter Map structure and cross-chapter coherence pass)
    - references/book-dna-template.md (current Chapter Map schema)
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md (D-10 through D-13)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md Pitfall 9 (central_image collisions)
  </read_first>
  <files>
    skills/outliner/SKILL.md
    references/book-dna-template.md
  </files>
  <action>
    Outliner SKILL.md changes (additive — do not renumber sections):

    1. In the section defining per-chapter outline fields (currently title, hook, key_arguments, supporting_scriptures, momentum_position), add two new fields:
       - central_image: string. One dominant sensory anchor (examples: "a drowning man reaching for a rope", "the cold coffee on the hospital waiting-room table", "a lighthouse visible through fog"). This image must be threaded through the writer's opening 200 words, middle third, and closing 200 words of the chapter.
       - vulnerability_beat_seed: string. A pointer to real source material formatted as source_path:line (examples: sources/sermon-2024-03-15.md:42, book-dna.md:89, voice-profile.md:12). The writer MUST draw on this line when composing the chapter's first-person vulnerability beat. Fabricated beats (unsourced) are a CRAFT-04 failure.

    2. Add a new subsection titled "Central Image Distinctness Check" in the outliner's cross-chapter coherence pass (place it after any existing cross-chapter coherence step). The subsection must say:
       After assigning central_image to every chapter, re-read the full list and verify no two chapters share a near-identical image. Use semantic judgment, not string match: "flickering candle" vs "dim candle" is too similar; "drowning man" vs "lighthouse in fog" is distinct enough. If any pair is too similar, revise one of them. Rationale: see .planning/research/PITFALLS.md Pitfall 9 (central_image collisions).

    3. Add a new subsection titled "Vulnerability Beat Seed Sourcing" explaining that for each chapter the outliner must walk sources/, sources-adapted/, voice-profile.md, and the Book DNA to find a real fragment that can seed the chapter's vulnerability beat. If no fragment can be found, leave the field empty AND add a note so the writer skips the beat for that chapter.

    4. Update the Book DNA emission step so the outliner writes central_image and vulnerability_beat_seed into the Book DNA Chapter Map alongside title, hook, key_arguments, supporting_scriptures, momentum_position.

    references/book-dna-template.md changes:

    5. Update the Chapter Map to include central_image and vulnerability_beat_seed per chapter. If the current Chapter Map is a table, add two columns. If cells get too wide, represent each chapter as a sub-list (title, hook, ..., central_image, vulnerability_beat_seed). Planner's judgment on layout — the constraint is that both fields MUST appear per chapter entry.
  </action>
  <verify>
    <automated>grep -q "central_image" skills/outliner/SKILL.md && grep -q "vulnerability_beat_seed" skills/outliner/SKILL.md && grep -q "central_image" references/book-dna-template.md && grep -q "vulnerability_beat_seed" references/book-dna-template.md && grep -q "Central Image Distinctness" skills/outliner/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "central_image" skills/outliner/SKILL.md` returns at least 3
    - `grep -c "vulnerability_beat_seed" skills/outliner/SKILL.md` returns at least 3
    - `grep -q "Central Image Distinctness" skills/outliner/SKILL.md` is true
    - `grep -q "Vulnerability Beat Seed Sourcing" skills/outliner/SKILL.md` is true
    - `grep -c "central_image" references/book-dna-template.md` returns at least 1
    - `grep -c "vulnerability_beat_seed" references/book-dna-template.md` returns at least 1
  </acceptance_criteria>
  <done>
    Outliner produces distinct central_images and sourced vulnerability seeds; book-dna-template carries both fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update writer skill with scene-first, Greek cap, central image, vulnerability, reader moments, pulpit avoidance, provenance</name>
  <read_first>
    - skills/writer/SKILL.md (full file, 351 lines)
    - references/bestseller-craft-rules.md (authoritative rule text — writer cites by path)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md Q2 (writer insertion map with exact sections)
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md (D-11, D-12, D-14, D-19)
  </read_first>
  <files>skills/writer/SKILL.md</files>
  <action>
    All additions are additive — no section renumbering. Insertion targets from 10-RESEARCH Q2:

    1. Section 1 "On Invocation" (lines 12-61): Add a step after reading Book DNA / voice profile / outline / research. The step says: Read the chapter's central_image and vulnerability_beat_seed from the outline/Book DNA. These are constraints, not suggestions. central_image must be threaded through the opening 200 words, middle third, and closing 200 words. vulnerability_beat_seed must resolve to a real source file + line — do NOT invent a vulnerability beat if the seed field is empty; in that case, skip the beat and note the skip in METADATA.

    2. Section 3 "Hook Strategies" (lines 76-108): Add a new subsection titled "Scene-First Opener Requirements (CRAFT-01)" immediately after the existing hook-types list. The subsection states:

       Mandatory first line of every chapter draft (exact format):
           <!-- provenance: {source_path}:{line} -->
       where source_path is one of sources/{file}.md, sources-adapted/{file}.md, book-dna.md, or voice-profile.md, and line is the integer line number you drew the opening scene from. If your scene is synthesised from multiple sources, pick the primary one. The comment is stripped by the formatter before .docx emission.

       Within the first 150 words you MUST include: (1) a named human (proper noun) or first-person narrator ("I", "me", "my"); (2) a time-marker (examples: "at 2am", "last Tuesday", "the summer I was fourteen"); (3) a sensory or physical detail (light, sound, texture, smell, or a specifically named object).

       See ${CLAUDE_PLUGIN_ROOT}/references/bestseller-craft-rules.md § CRAFT-01 for the authoritative spec. Missing provenance = editor auto-revise. Missing any of (1)(2)(3) = editor flag.

    3. Section 4 "Chapter Structure — tension-release" (lines 110-146): Add two new subsections at the end of Section 4:

       "Central Image Discipline (CRAFT-03)": Thread the chapter's central_image (from outline) through three zones — opening 200 words, middle third, closing 200 words. Do not pick a new image; do not drop it mid-chapter. Present each zone's use in a different register (literal description in opening, metaphor in middle, echo in closing). See ${CLAUDE_PLUGIN_ROOT}/references/bestseller-craft-rules.md § CRAFT-03.

       "Vulnerability Beat (CRAFT-04)": Place exactly one first-person vulnerability beat in the middle third of the chapter, sourced from the outline's vulnerability_beat_seed. Open the line of source material the seed points to, and compose the beat from that material. Never fabricate a vulnerability beat — if the seed is empty, skip the beat and record `vulnerability_beat: skipped` in METADATA. Fabrication is a CRAFT-04 hard fail.

    4. Section 6 "Momentum-Aware Pacing" (lines 191-213): Add a subsection "Reader Moments Selection (CRAFT-06)": Read the voice profile's Reader Moments section (if present). Select ≥2 concrete moments and weave them into the chapter as specific anchors for any abstract claim. Record which moments you selected in METADATA (example: `reader_moments_used: ["the 2am phone-check", "the grocery-aisle grief flash"]`). If the voice profile has no Reader Moments section, skip this rule and record `reader_moments_used: []` in METADATA (editor runs CRAFT-06 in flag-only mode).

    5. Section 7 "Theological Depth" (lines 216-254): Add at the TOP of the section, before any existing content, a subsection titled "Transliterated Term Density Cap (CRAFT-02)":

       Maximum 3 distinct transliterated Greek/Hebrew terms per chapter. Each term MUST receive at least 3 sentences of unpacking in the same paragraph block (the next 3 sentences after you introduce the term). Unpacking sentences must contain explanatory markers such as "means", "carries", "literally", "in Greek", "the word", "this is".

       Terms counted against the cap (authoritative lexicon — kept in sync with scripts/craft-check.js and references/bestseller-craft-rules.md): charis, agape, phileo, eros, storge, dunamis, exousia, logos, rhema, pneuma, sarx, kairos, chronos, sunergeo, pas, shalom, hesed, chesed, ruach, yada, ahavah, nephesh, echad, koinonia, metanoia.

       Over-cap = editor auto-revise. Prefer English over transliteration whenever semantic fidelity is preserved.

    6. Section 8 "Output Format" (lines 308-338): Update the METADATA block documentation to add these fields: central_image, vulnerability_beat, vulnerability_beat_source, reader_moments_used, provenance. Also state that the first two lines of every chapter draft MUST be, in this exact order:
           <!-- provenance: {source_path}:{line} -->
           <!-- generated-by: book-crafter v1.1.0 -->
       Followed by the chapter heading `# Chapter N: Title` on line 3 or later.

    7. Section 9 "Anti-Patterns" (lines 340-351): Add three bullets:
       - No paragraph (and no chapter) may start with: "So", "Now", "And so", "Let us", "Let me", "Here's where", "Here's the thing", "You see", "Listen", "Church", "Friend". Mid-paragraph uses are fine. See references/bestseller-craft-rules.md § CRAFT-05.
       - Never fabricate a first-person vulnerability beat. If vulnerability_beat_seed is empty, skip the beat.
       - Never exceed 3 distinct transliterated Greek/Hebrew terms per chapter.

    All insertions are additive. Do not remove existing writer content.
  </action>
  <verify>
    <automated>grep -q "Scene-First Opener" skills/writer/SKILL.md && grep -q "Central Image Discipline" skills/writer/SKILL.md && grep -q "Vulnerability Beat" skills/writer/SKILL.md && grep -q "Reader Moments Selection" skills/writer/SKILL.md && grep -q "Transliterated Term Density Cap" skills/writer/SKILL.md && grep -q "bestseller-craft-rules.md" skills/writer/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Scene-First Opener Requirements" skills/writer/SKILL.md` is true
    - `grep -q "Central Image Discipline" skills/writer/SKILL.md` is true
    - `grep -q "Vulnerability Beat" skills/writer/SKILL.md` is true
    - `grep -q "Reader Moments Selection" skills/writer/SKILL.md` is true
    - `grep -q "Transliterated Term Density Cap" skills/writer/SKILL.md` is true
    - `grep -c "bestseller-craft-rules.md" skills/writer/SKILL.md` returns at least 3
    - `grep -q "provenance:" skills/writer/SKILL.md` is true
    - `grep -q "vulnerability_beat_seed" skills/writer/SKILL.md` is true
    - `grep -c '"charis"\|charis,' skills/writer/SKILL.md` returns at least 1 (lexicon present)
    - `grep -q "reader_moments_used" skills/writer/SKILL.md` is true
    - Writer file line count is between 400 and 520 (grew by roughly 70 lines from 351)
  </acceptance_criteria>
  <done>
    Writer skill enforces CRAFT-01..06 procedurally at draft time. Chapter drafts now carry provenance + version stamp on lines 1-2, thread central image, place one sourced vulnerability beat, use ≥2 reader moments, cap Greek density, and avoid pulpit-seam starts.
  </done>
</task>

</tasks>

<verification>
- Writer produces drafts that pass craft-check.js CRAFT-01/02/05/07/15 on the fixture set (verified in Plan 1's test suite)
- Outliner emits central_image and vulnerability_beat_seed per chapter, distinct across chapters
- Book DNA template carries both fields
</verification>

<success_criteria>
A newly-generated chapter now has: first-line provenance comment, version stamp on line 2, named human + time-marker + sensory detail in first 150 words, central image woven through three zones, one sourced vulnerability beat, ≥2 reader moments from voice profile, ≤3 transliterated terms, no pulpit-seam paragraph starts.
</success_criteria>

<output>
After completion, create `.planning/phases/10-writing-quality-v2/10-03-SUMMARY.md`
</output>
