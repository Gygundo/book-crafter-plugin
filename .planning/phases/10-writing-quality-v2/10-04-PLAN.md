---
phase: 10-writing-quality-v2
plan: 04
type: execute
wave: 4
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - skills/editor/SKILL.md
autonomous: true
requirements: [CRAFT-02, CRAFT-05, CRAFT-07, CRAFT-08]
must_haves:
  truths:
    - "Editor Pass 1 invokes craft-check.js at the start of each chapter and merges JSON results into VOICE AUDIT"
    - "Editor Pass 1 includes new sub-sections for craft density, pulpit seam detection, tension-release enforcement, and show-don't-tell audit"
    - "Pulpit seam and Greek overflow trigger auto-revise; other checks flag only"
  artifacts:
    - path: "skills/editor/SKILL.md"
      provides: "Editor Pass 1 with deterministic craft-check invocation + new LLM judgment sub-sections"
      contains: "craft-check.js"
  key_links:
    - from: "skills/editor/SKILL.md"
      to: "scripts/craft-check.js"
      via: "Bash invocation at Pass 1 start"
      pattern: "craft-check\\.js"
---

<objective>
Extend editor Pass 1 with deterministic craft-check.js invocation and four new LLM judgment sub-sections — craft density (CRAFT-02 unpacking adequacy), pulpit seam detection (CRAFT-05 auto-revise), tension-release enforcement (CRAFT-07 reader-thought lines), and show-don't-tell audit (CRAFT-08 concrete:abstract ratio).

Purpose: Catch deterministic CRAFT failures at Pass 1 entry (before LLM work begins) and run judgment checks for rules the script cannot evaluate. Per D-06, hard gates (CRAFT-02/05) auto-revise; per D-07, judgment rules (CRAFT-07/08) flag only.
Output: Updated editor SKILL.md with new Pass 1 sub-sections 2.9, 2.10, 2.11, and a show-don't-tell audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-writing-quality-v2/10-CONTEXT.md
@.planning/phases/10-writing-quality-v2/10-RESEARCH.md
@skills/editor/SKILL.md
@references/bestseller-craft-rules.md
@scripts/craft-check.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add craft-check.js invocation + 4 new Pass 1 sub-sections to editor</name>
  <read_first>
    - skills/editor/SKILL.md (full file — especially Section 2 "Pass 1" lines 66-215)
    - .planning/phases/10-writing-quality-v2/10-RESEARCH.md Q4 (Phase 10 insertion map for Pass 1 sub-sections) and "Pattern 1: Skill-Invoked Bash Checker"
    - .planning/phases/10-writing-quality-v2/10-CONTEXT.md (D-02, D-03, D-04, D-06, D-07)
    - references/bestseller-craft-rules.md (authoritative rule text)
  </read_first>
  <files>skills/editor/SKILL.md</files>
  <action>
    Additions are purely additive to Pass 1 (Section 2). Do not remove or renumber existing 2.1-2.8 sub-sections.

    1. Add a new sub-section "2.0 Craft Check Invocation (deterministic)" as the FIRST step of Pass 1, before 2.1. It must instruct:

       At the start of Pass 1 for each chapter, invoke the deterministic craft checker before any LLM work:

           node ${CLAUDE_PLUGIN_ROOT}/scripts/craft-check.js [project]/drafts/ch[NN]-draft.md

       Parse the JSON output. The schema is:
           {
             "chapter_id": "ch01",
             "checks": {
               "CRAFT-01": { "pass": bool, "evidence": "...", "citations": [...] },
               "CRAFT-02": { ... },
               "CRAFT-05": { ... },
               "CRAFT-07": { ... },
               "CRAFT-15": { ... }
             }
           }

       Merge every check result into the chapter's `<!-- VOICE AUDIT -->` metadata block under a new field `craft_check`.

       Policy (from D-06 and D-07):
       - If CRAFT-01 fails (missing/malformed provenance OR unresolvable path) → auto-revise: request writer rewrite of the chapter opener only, preserving the rest of the draft.
       - If CRAFT-02 fails (distinct terms > 3) → auto-revise: request writer rewrite to cut terms to ≤3, each with ≥3 unpacking sentences.
       - If CRAFT-05 fails (pulpit-seam at any paragraph start) → auto-revise: request writer rewrite of the specific paragraph(s), keeping the rest of the chapter.
       - If CRAFT-07 fails (<2 reader-thought lines) → FLAG ONLY. Add to diagnostic report. Do not auto-revise.
       - If CRAFT-15 fails (missing version stamp) → auto-fix by prepending the stamp; do not auto-revise writer.
       - All revision requests respect the 2-revision cap wired in Plan 9 (CRAFT-17). On exhaustion, keep the highest-scoring revision and flag in diagnostic report.

    2. Add sub-section "2.9 Craft Density Check (CRAFT-02 unpacking adequacy)" at the END of Pass 1 (after 2.8). Content:

       This runs AFTER craft-check.js has already confirmed CRAFT-02 distinct-term count is ≤3. This sub-section performs LLM judgment on unpacking adequacy: for each transliterated term used, confirm the next 3 sentences after its introduction actually unpack it with explanatory context (meaning, etymology, or concrete illustration) — not just mention it.

       Failure mode: flag in diagnostic report with the term and line. No auto-revise (LLM judgment is advisory, per D-07 rationale).

       Rule reference: ${CLAUDE_PLUGIN_ROOT}/references/bestseller-craft-rules.md § CRAFT-02.

    3. Add sub-section "2.10 Pulpit Seam Detection (CRAFT-05)" at the END of Pass 1:

       This is the LLM counterpart to craft-check.js's deterministic regex. craft-check.js has already flagged any paragraph starting with a banned word. Editor reviews each flagged paragraph against the permitted-usage counter-example list in bestseller-craft-rules.md § CRAFT-05 (dialogue/quotation, scripture citation, titled section, second-person remembered scene). If the flag matches a permitted usage, override the craft-check fail to a pass and note the override in the VOICE AUDIT. If the flag does not match any permitted usage, confirm the fail and trigger auto-revise of that paragraph only.

       Rule reference: ${CLAUDE_PLUGIN_ROOT}/references/bestseller-craft-rules.md § CRAFT-05.

    4. Add sub-section "2.11 Tension-Release Enforcement (CRAFT-07)" at the END of Pass 1:

       craft-check.js has counted italicised/blockquote reader-thought lines. If the count is <2, flag in diagnostic report: "Chapter N has only X reader-thought lines; expected ≥2." Do not auto-revise (per D-07). Editor may suggest candidate insertion points during Pass 2 rewrite windows.

       Rule reference: ${CLAUDE_PLUGIN_ROOT}/references/bestseller-craft-rules.md § CRAFT-07.

    5. Add sub-section "2.12 Show-Don't-Tell Audit (CRAFT-08)" at the END of Pass 1:

       LLM judgment sub-section. For the chapter, slide a 4-paragraph window through the text. In each window, count concrete nouns (physical objects, named people, named places, sensory words) vs abstract nouns (grace, identity, righteousness, faith, hope, salvation, sonship, authority, kingdom, anointing — see bestseller-craft-rules.md § CRAFT-08 for the full hint list). Ratio must be ≥1:1. Flag any window where abstract outnumbers concrete with the window's paragraph range.

       Failure mode: FLAG ONLY. Do not auto-revise.

       Rule reference: ${CLAUDE_PLUGIN_ROOT}/references/bestseller-craft-rules.md § CRAFT-08.

    6. Update §2.8 (Pass 1 Output — VOICE AUDIT block) to include the `craft_check` merged field. Example shape to document:
           <!-- VOICE AUDIT -->
           captivation_score: N
           craft_check:
             CRAFT-01: pass
             CRAFT-02: pass (2 distinct terms: charis, dunamis; unpacking OK)
             CRAFT-05: pass
             CRAFT-07: fail (1 reader-thought line; flagged)
             CRAFT-15: pass
             CRAFT-08: fail (window p12-p15 ratio 0.6; flagged)
           <!-- /VOICE AUDIT -->

    Do not touch Pass 2, Pass 3, or any existing rubric invocation. Those are handled by Plan 5 and Plan 6.
  </action>
  <verify>
    <automated>grep -q "2.0 Craft Check Invocation" skills/editor/SKILL.md && grep -q "2.9 Craft Density" skills/editor/SKILL.md && grep -q "2.10 Pulpit Seam" skills/editor/SKILL.md && grep -q "2.11 Tension-Release" skills/editor/SKILL.md && grep -q "2.12 Show-Don't-Tell" skills/editor/SKILL.md && grep -q "craft-check.js" skills/editor/SKILL.md && grep -q "craft_check:" skills/editor/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "2.0 Craft Check Invocation" skills/editor/SKILL.md` is true
    - `grep -q "2.9 Craft Density" skills/editor/SKILL.md` is true
    - `grep -q "2.10 Pulpit Seam" skills/editor/SKILL.md` is true
    - `grep -q "2.11 Tension-Release" skills/editor/SKILL.md` is true
    - `grep -q "2.12 Show-Don't-Tell" skills/editor/SKILL.md` is true
    - `grep -c "craft-check.js" skills/editor/SKILL.md` returns at least 3
    - `grep -c "bestseller-craft-rules.md" skills/editor/SKILL.md` returns at least 4
    - `grep -q "craft_check:" skills/editor/SKILL.md` is true (VOICE AUDIT block example)
    - `grep -q "permitted-usage" skills/editor/SKILL.md || grep -q "permitted usage" skills/editor/SKILL.md` is true
    - Existing captivation-rubric.md references from Plan 1 Task 3 still present: `grep -c "captivation-rubric.md" skills/editor/SKILL.md` returns at least 5
  </acceptance_criteria>
  <done>
    Editor Pass 1 now runs craft-check.js first, parses JSON, enforces auto-revise on CRAFT-01/02/05 failures, flags CRAFT-07/08 in VOICE AUDIT, and has explicit LLM judgment sub-sections for craft density, pulpit seam override, tension-release, and show-don't-tell.
  </done>
</task>

</tasks>

<verification>
- `node scripts/craft-check.js fixtures/phase10/known-bad/ch01-pulpit.md` still fails CRAFT-05 (Plan 1 infrastructure unaffected)
- Editor skill file grew by approximately 80-120 lines; existing content unchanged
</verification>

<success_criteria>
Editor Pass 1 has a working hybrid enforcement model: deterministic craft-check.js at entry + LLM judgment sub-sections for rules the script cannot evaluate, with clear auto-revise/flag-only policy per D-06/D-07.
</success_criteria>

<output>
After completion, create `.planning/phases/10-writing-quality-v2/10-04-SUMMARY.md`
</output>
