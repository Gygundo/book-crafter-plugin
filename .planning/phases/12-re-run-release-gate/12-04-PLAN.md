---
phase: 12-re-run-release-gate
plan: 04
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - evidence/external-review.md
autonomous: false
requirements: [GATE-04]

must_haves:
  truths:
    - "A fresh Claude session ranked before vs after blind without knowing which was which"
    - "The full exchange (prompt + response + reveal) is logged as evidence"
  artifacts:
    - path: "evidence/external-review.md"
      provides: "Third-party blind quality ranking"
      contains: "## Reveal"
  key_links:
    - from: "evidence/external-review.md"
      to: "evidence/eternally-secure-ch1-before.md"
      via: "Version A or B assignment"
      pattern: "Version [AB].*before"
---

<objective>
Conduct a blind third-party review by a fresh Claude session comparing the before and after Ch1 versions.

Purpose: Independent quality signal from a reviewer who does not know which version is the v1.1 output. This prevents confirmation bias in the evidence package.
Output: evidence/external-review.md with the full exchange logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-re-run-release-gate/12-CONTEXT.md
@.planning/phases/12-re-run-release-gate/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the blind review prompt and version assignment</name>
  <files>evidence/external-review.md</files>
  <read_first>
    evidence/eternally-secure-ch1-before.md
    evidence/eternally-secure-ch1-after.md
  </read_first>
  <action>
    Per D-09: Randomise the assignment of before/after to Version A/B. Use a coin-flip method (e.g., `echo $((RANDOM % 2))` — 0 means before=A/after=B, 1 means before=B/after=A). Record the mapping.

    Per D-10 (Claude's Discretion): Design a prompt that asks for a quality ranking with reasoning across multiple dimensions. Suggested dimensions: opening engagement, prose quality, voice consistency, theological depth, reader connection, overall readability.

    Create the initial `evidence/external-review.md` file with:

    ```markdown
    # External Blind Review (GATE-04)

    **Date:** [date]
    **Method:** Fresh Claude Code session, no project context, randomised A/B assignment
    **Randomisation:** [record: before=[A or B], after=[A or B]]

    ## Prompt Sent

    [The exact prompt text, with Version A and Version B chapter texts embedded]

    ## Response

    [To be filled after the review session]

    ## Reveal

    - Version A = [before or after] (evidence/eternally-secure-ch1-[before or after].md)
    - Version B = [before or after] (evidence/eternally-secure-ch1-[before or after].md)
    ```

    The prompt should be structured as:

    ```
    I have two versions of a book chapter on eternal security (a Christian theology topic). Please rank them on quality across these dimensions:

    1. Opening engagement (does it hook you immediately?)
    2. Prose quality (sentence variety, rhythm, show-don't-tell)
    3. Concrete imagery (specific scenes, sensory details, named people)
    4. Reader connection (does it feel personal, or like a lecture?)
    5. Overall: which version would you keep reading?

    For each dimension, identify the stronger version and explain why with specific quotes.

    ---

    ## VERSION A

    [full chapter text]

    ---

    ## VERSION B

    [full chapter text]
    ```

    Embed the FULL text of both chapters in the prompt (not summaries).
  </action>
  <verify>
    <automated>test -f evidence/external-review.md && grep -q "## Prompt Sent" evidence/external-review.md && grep -q "VERSION A" evidence/external-review.md && grep -q "VERSION B" evidence/external-review.md && echo "VERIFY PASS"</automated>
  </verify>
  <acceptance_criteria>
    - evidence/external-review.md exists
    - Contains "## Prompt Sent" section with the full prompt text
    - Contains both "VERSION A" and "VERSION B" with embedded chapter texts
    - Contains "## Reveal" section with the A/B mapping
    - The randomisation method is documented
  </acceptance_criteria>
  <done>Blind review prompt prepared with randomised version assignment</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute the blind review in a fresh Claude session</name>
  <action>
    David opens a fresh Claude Code session (or claude.ai conversation) with NO project context loaded -- no CLAUDE.md, no book-crafter plugin context, clean conversation.

    Per D-09 and D-11: Paste the prompt from evidence/external-review.md into the fresh session. Copy the full response back into the "## Response" section of evidence/external-review.md.
  </action>
  <instructions>
    1. Open a NEW Claude conversation (claude.ai or fresh Claude Code session with no project loaded)
    2. Copy the entire "## Prompt Sent" section from `evidence/external-review.md` and paste it into the fresh session
    3. Wait for the full response
    4. Copy the complete response
    5. Paste it into the "## Response" section of `evidence/external-review.md`
    6. Save the file
    7. Type "review complete" to resume
  </instructions>
  <resume-signal>Type "review complete" after pasting the response into evidence/external-review.md</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validate the external review evidence file</name>
  <files>evidence/external-review.md</files>
  <read_first>
    evidence/external-review.md
  </read_first>
  <action>
    Read the completed evidence/external-review.md and verify:
    1. The "## Response" section is populated (not empty or placeholder)
    2. The "## Reveal" section correctly maps Version A/B to before/after
    3. The response contains quality assessments (look for dimension-specific feedback)

    If the Response section is still a placeholder, flag this as incomplete.

    No modifications to the reviewer's response -- log it verbatim. Only ensure the Reveal section has the correct mapping at the bottom.
  </action>
  <verify>
    <automated>grep -q "## Response" evidence/external-review.md && grep -q "## Reveal" evidence/external-review.md && ! grep -q "To be filled" evidence/external-review.md && echo "VERIFY PASS"</automated>
  </verify>
  <acceptance_criteria>
    - evidence/external-review.md contains a populated "## Response" section (no placeholder text)
    - "## Reveal" section maps Version A and Version B to before/after files
    - The response is the verbatim output from the fresh Claude session
  </acceptance_criteria>
  <done>External blind review completed and logged with full exchange, randomisation, and reveal</done>
</task>

</tasks>

<verification>
- evidence/external-review.md has populated Prompt, Response, and Reveal sections
- The reviewer's response contains quality assessments across multiple dimensions
- The A/B mapping is documented in the Reveal section
</verification>

<success_criteria>
GATE-04 satisfied: fresh-Claude blind ranking of before vs after logged to evidence/external-review.md with full exchange and reveal.
</success_criteria>

<output>
After completion, create `.planning/phases/12-re-run-release-gate/12-04-SUMMARY.md`
</output>
