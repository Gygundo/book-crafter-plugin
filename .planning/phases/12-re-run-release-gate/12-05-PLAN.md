---
phase: 12-re-run-release-gate
plan: 05
type: execute
wave: 4
depends_on: [12-03]
files_modified:
  - README.md
  - evidence/smoke-test-log.md
autonomous: false
requirements: [GATE-06, GATE-07]

must_haves:
  truths:
    - "README capability language is evidence-anchored against the seven-gap comparison"
    - "Fresh-install smoke-test passes: marketplace install produces a valid .docx on first attempt"
    - "All evidence and final README are pushed to GitHub before the smoke test"
  artifacts:
    - path: "README.md"
      provides: "Finalised capability language and fixture paragraph"
      min_lines: 30
    - path: "evidence/smoke-test-log.md"
      provides: "Fresh-install test evidence"
      contains: "SMOKE TEST:"
  key_links:
    - from: "README.md"
      to: "evidence/seven-gap-comparison.md"
      via: "capability language anchored to evidence"
      pattern: "no.*bestseller.*claim.*without.*evidence"
---

<objective>
Finalise README capability language against the seven-gap evidence, push all commits to GitHub, then run the fresh-install smoke test.

Purpose: GATE-07 ensures no overclaiming (Pitfall 22). GATE-06 validates the full install-from-marketplace path works for a recipient. These must happen after evidence gathering (Wave 3) and before the ship decision (Wave 5).
Output: Updated README.md, evidence/smoke-test-log.md, all commits pushed to GitHub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-re-run-release-gate/12-CONTEXT.md
@.planning/phases/12-re-run-release-gate/12-03-SUMMARY.md
@.planning/phases/11-distribution-packaging/11-02-SUMMARY.md
</context>

<interfaces>
<!-- Current README structure from Phase 11 -->
From README.md:
- Line 5: `Writes structured non-fiction books with enforced craft rules.` (capability line to update)
- Lines 7-8: `<!-- TODO(phase-12): ... -->` placeholder block (to replace with fixture paragraph)
- Lines 12-16: Install block (preserve exactly)
- Lines 18-40: Rest of README (preserve)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Finalise README capability language and fixture paragraph</name>
  <files>README.md</files>
  <read_first>
    README.md
    evidence/seven-gap-comparison.md
    evidence/external-review.md
  </read_first>
  <action>
    Per D-18: Replace the Phase 11 placeholder capability line ("Writes structured non-fiction books with enforced craft rules.") with evidence-anchored language.

    Per D-19 / Pitfall 22: No "bestseller quality" claim unless the seven-gap comparison substantiates it. The capability line must be defensible against the evidence rows.

    Steps:
    1. Read `evidence/seven-gap-comparison.md` to determine what improvements are actually demonstrated
    2. Read `evidence/external-review.md` to see the blind reviewer's assessment
    3. Write a capability line that accurately reflects the demonstrated improvements. Examples of defensible language (choose based on actual evidence):
       - If all 7 gaps show clear improvement: "Writes non-fiction books with scene-driven openers, controlled theological density, and voice-consistent prose -- enforced by 8 countable craft rules."
       - If mixed results: "Writes structured non-fiction books with enforced craft rules covering opener quality, imagery, and voice consistency."
       - Adapt based on what the evidence actually shows

    4. Replace the `<!-- TODO(phase-12): ... -->` block (lines 7-8 in current README) with a 3-4 line quoted prose sample from the re-run output. Pull a representative paragraph from `evidence/eternally-secure-ch1-after.md` that showcases the improvement (e.g., the scene opener).

    5. Preserve ALL other README content exactly: install block, "How to run it", "What this makes", "Try the built-in sample", "Licence", "Changelog" sections.

    CRITICAL: Do NOT add "bestseller" or "professional author" language unless the blind review explicitly uses those terms for the after version.
  </action>
  <verify>
    <automated>grep -v "TODO(phase-12)" README.md | grep -q ">" && grep -q "Install" README.md && grep -q "plugin marketplace add" README.md && ! grep -q "TODO(phase-12)" README.md && echo "VERIFY PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md does NOT contain "TODO(phase-12)"
    - README.md does NOT contain "bestseller" (unless the blind review used that exact term for the after version)
    - README.md contains a blockquote ("> ") prose sample from the re-run output
    - README.md still contains the exact install block: "plugin marketplace add gygundo/book-crafter-plugin"
    - README.md still contains "Requires:" and "Node" in the first line
    - README.md still contains "## Install", "## How to run it", "## What this makes"
  </acceptance_criteria>
  <done>README capability language updated with evidence-anchored claims; TODO placeholder replaced with real prose sample</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push to GitHub and run fresh-install smoke test</name>
  <files>evidence/smoke-test-log.md</files>
  <action>
    Push all commits to GitHub, then David runs the fresh-install smoke test per D-14 through D-17. Claude pushes with `git push origin main` before the checkpoint. David then performs the manual smoke test steps below.
  </action>
  <what-built>
    All Phase 12 evidence files and the finalised README have been committed and pushed to GitHub.
  </what-built>
  <how-to-verify>
    **Smoke test procedure (per D-16):**
    1. Remove the dev plugin: uninstall `book-crafter-dev` from Claude Code
    2. Clear Claude Code plugin cache (restart Claude Code or clear cache directory)
    3. Install from marketplace using the README's 3-command block:
       ```
       /plugin marketplace add gygundo/book-crafter-plugin
       /plugin install book-crafter@book-crafter-plugin
       /reload-plugins
       ```
    4. Run `/book-crafter:sample` and wait for it to complete
    5. Verify: sample reports PASS (captivation score meets threshold + novelty_dedup pass)
    6. Verify: a .docx file exists in the fixture output directory
    7. After smoke test, reinstall the dev plugin

    **Record results:** Type the outcome -- "smoke test PASS" or describe any failures.
  </how-to-verify>
  <verify>
    <automated>echo "CHECKPOINT: awaiting human verification of smoke test"</automated>
  </verify>
  <done>Smoke test completed and result reported by David</done>
  <resume-signal>Type "smoke test PASS" or describe failures</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record smoke test evidence</name>
  <files>evidence/smoke-test-log.md</files>
  <read_first>
    evidence/smoke-test-log.md
  </read_first>
  <action>
    Create `evidence/smoke-test-log.md` based on the smoke test results from Task 2.

    Structure:
    ```markdown
    # Fresh-Install Smoke Test (GATE-06)

    **Date:** [date]
    **Machine:** David's Mac (dev machine)
    **Method:** Remove dev plugin -> clear cache -> marketplace install -> /book-crafter:sample -> verify .docx

    ## Procedure

    1. Removed `book-crafter-dev` dev plugin
    2. Cleared Claude Code plugin cache
    3. Installed from marketplace:
       - `/plugin marketplace add gygundo/book-crafter-plugin`
       - `/plugin install book-crafter@book-crafter-plugin`
       - `/reload-plugins`
    4. Ran `/book-crafter:sample`
    5. Verified output

    ## Results

    - Sample skill exit: [PASS/FAIL]
    - Captivation score: [score if available]
    - Novelty/dedup: [PASS/FAIL if available]
    - .docx exists: [YES/NO]
    - First attempt: [YES/NO]

    ## SMOKE TEST: [PASS/FAIL]

    [Brief notes on any issues encountered]
    ```

    Fill in actual results based on what David reported in the checkpoint.
    If the smoke test failed, record the failure details and flag as a blocker.
  </action>
  <verify>
    <automated>test -f evidence/smoke-test-log.md && grep -q "SMOKE TEST:" evidence/smoke-test-log.md && echo "VERIFY PASS"</automated>
  </verify>
  <acceptance_criteria>
    - evidence/smoke-test-log.md exists
    - Contains "SMOKE TEST: PASS" or "SMOKE TEST: FAIL"
    - Contains the procedure steps
    - Contains results for: sample skill exit, .docx exists, first attempt
  </acceptance_criteria>
  <done>Fresh-install smoke test evidence recorded; GATE-06 result documented</done>
</task>

</tasks>

<verification>
- README.md has evidence-anchored capability language (no overclaiming)
- README.md TODO placeholder is replaced with real prose sample
- evidence/smoke-test-log.md documents the fresh-install test result
- All commits pushed to GitHub
</verification>

<success_criteria>
GATE-06 satisfied: fresh-install smoke-test passed (or failure documented).
GATE-07 satisfied: README capability language finalised against comparison evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/12-re-run-release-gate/12-05-SUMMARY.md`
</output>
