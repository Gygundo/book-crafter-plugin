---
phase: 02-voice-system-book-outliner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - references/voice-profiles/voice-profile-spec.md
  - references/voice-profiles/spiritual-default.md
  - skills/orchestrator/SKILL.md
autonomous: true
requirements: [VOICE-01, VOICE-02, VOICE-03, VOICE-04]

must_haves:
  truths:
    - "A voice profile spec document defines the required and optional sections every voice profile must contain"
    - "The default spiritual voice profile contains all required sections defined in the spec"
    - "The orchestrator accepts a custom .md file path as voice profile and copies it to the project"
    - "The orchestrator accepts an inline voice description and generates a full voice profile from it"
    - "When no voice is specified, the orchestrator uses spiritual-default.md"
  artifacts:
    - path: "references/voice-profiles/voice-profile-spec.md"
      provides: "Voice profile specification defining required and optional sections"
      contains: "## Required Sections"
    - path: "references/voice-profiles/spiritual-default.md"
      provides: "Default theological voice profile"
      contains: "## Anti-Patterns"
    - path: "skills/orchestrator/SKILL.md"
      provides: "Updated orchestrator with voice profile selection logic"
      contains: "Inline description"
  key_links:
    - from: "skills/orchestrator/SKILL.md"
      to: "references/voice-profiles/"
      via: "voice profile selection and copying"
      pattern: "voice-profile-spec"
    - from: "skills/orchestrator/SKILL.md"
      to: "[project]/voice-profile.md"
      via: "copying selected profile to project directory"
      pattern: "voice-profile.md"
---

<objective>
Create the voice profile system: a specification document defining voice profile structure, verify/enhance the default spiritual profile against the spec, and update the orchestrator to handle all four voice input modes (named profile, custom file, inline description, no voice specified).

Purpose: Voice profiles are the foundation of voice consistency. Every downstream skill and agent reads the voice profile. Without a clear spec, custom profiles will have inconsistent structures that break Book DNA generation. Without inline voice support, users must create .md files for one-off projects.

Output: voice-profile-spec.md, enhanced spiritual-default.md, orchestrator SKILL.md with voice selection logic
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-voice-system-book-outliner/02-RESEARCH.md

<interfaces>
<!-- Key contracts from Phase 1 that this plan depends on -->

From skills/orchestrator/SKILL.md (Section 2 - Project Creation):
- Gathers: Book title, topic brief, key themes, target audience, book size tier, voice profile
- Voice profile default: "spiritual-default" or path to custom .md
- Copies selected voice profile to [project]/voice-profile.md
- Uses ${CLAUDE_PLUGIN_ROOT}/references/voice-profiles/ for profile lookup

From references/voice-profiles/spiritual-default.md (current sections):
- Tone, Sentence Patterns, Vocabulary (Use/Avoid), Emphasis Techniques
- Theological Framework, Anti-Patterns, Scripture Handling

From references/book-dna-template.md (Voice Profile section):
- Expects: Tone, Sentence Patterns, Vocabulary (Use/Avoid), Emphasis Techniques
- Expects: Theological/Domain Framework (separate section)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create voice profile spec and verify default profile</name>
  <files>references/voice-profiles/voice-profile-spec.md, references/voice-profiles/spiritual-default.md</files>
  <read_first>
    - references/voice-profiles/spiritual-default.md
    - references/book-dna-template.md
  </read_first>
  <action>
Create `references/voice-profiles/voice-profile-spec.md` with the following exact content structure:

```
# Voice Profile Specification

> Every voice profile (.md file in references/voice-profiles/) must follow this structure.
> The outliner validates profiles against this spec and fills missing optional sections with defaults.

## Required Sections

A voice profile is valid when sections 1-5 are present and non-empty.

### 1. Tone
Overall tonal quality. Describe the voice's character: bold/gentle, direct/reflective, formal/conversational, authoritative/exploratory. At least 2-3 sentences.

### 2. Sentence Patterns
Rhythm and structure guidance. Include:
- Average sentence length target
- Fragment usage (frequent/rare/never)
- Rhetorical question usage
- Repetition patterns
- Intensity building patterns

### 3. Vocabulary
Split into two subsections:

#### Use
Words, phrases, and language patterns characteristic of this voice. At least 5 examples.

#### Avoid
Words, phrases, and patterns that break this voice. At least 5 examples.

### 4. Emphasis Techniques
How the voice creates impact. At least 3 techniques with brief descriptions.

### 5. Anti-Patterns (Never Do This)
Behaviours that break the voice. At least 3 anti-patterns. These are the guardrails that prevent voice drift during parallel chapter generation.

## Optional Sections

### 6. Theological/Domain Framework
Interpretive lens for domain-specific content. Required for theological books. Contains the core beliefs, doctrinal positions, or domain expertise that shape content decisions. Omit for genre-neutral profiles.

### 7. Scripture Handling
Translation defaults, quoting style, citation conventions. Only for profiles that reference religious texts. Omit for secular profiles.

## Validation Rules

- Missing required section (1-5): WARN user, fill with "[Not specified -- using neutral, clear prose]" marked with <!-- DEFAULT -->
- Missing optional section (6-7): No warning, omit from Book DNA
- Empty required section (heading present but no content): Same as missing -- WARN and fill default
- Profile without any headings: REJECT -- not a valid voice profile

## Section Mapping to Book DNA

| Voice Profile Section | Book DNA Section |
|----------------------|------------------|
| Tone | Voice Profile > Tone |
| Sentence Patterns | Voice Profile > Sentence Patterns |
| Vocabulary | Voice Profile > Vocabulary |
| Emphasis Techniques | Voice Profile > Emphasis Techniques |
| Theological/Domain Framework | Theological/Domain Framework |
| Anti-Patterns | (Embedded in voice instructions to agents, not a separate Book DNA section) |
| Scripture Handling | Style Rules (merged) |
```

Then verify `spiritual-default.md` against the spec. The current file has all required sections (Tone, Sentence Patterns, Vocabulary with Use/Avoid, Emphasis Techniques, Anti-Patterns) and both optional sections (Theological Framework, Scripture Handling). No changes needed to spiritual-default.md content -- it already matches the spec. However, add the following comment at the top of the file, after the existing blockquote:

```
<!-- Validated against voice-profile-spec.md. All required sections (1-5) present. Optional sections (6-7) present. -->
```
  </action>
  <verify>
    <automated>grep -c "## Required Sections" references/voice-profiles/voice-profile-spec.md && grep -c "## Optional Sections" references/voice-profiles/voice-profile-spec.md && grep -c "## Validation Rules" references/voice-profiles/voice-profile-spec.md && grep "Validated against" references/voice-profiles/spiritual-default.md</automated>
  </verify>
  <acceptance_criteria>
    - voice-profile-spec.md contains "## Required Sections" heading
    - voice-profile-spec.md contains exactly 5 required sections numbered 1-5: Tone, Sentence Patterns, Vocabulary, Emphasis Techniques, Anti-Patterns
    - voice-profile-spec.md contains "## Optional Sections" heading with sections 6 (Theological/Domain Framework) and 7 (Scripture Handling)
    - voice-profile-spec.md contains "## Validation Rules" with rules for missing, empty, and invalid profiles
    - voice-profile-spec.md contains "## Section Mapping to Book DNA" table mapping profile sections to Book DNA sections
    - spiritual-default.md contains "Validated against voice-profile-spec.md" comment
    - spiritual-default.md retains all existing content (Tone, Sentence Patterns, Vocabulary, Emphasis Techniques, Theological Framework, Anti-Patterns, Scripture Handling)
  </acceptance_criteria>
  <done>Voice profile spec defines the contract for all voice profiles. Default spiritual profile validated against spec.</done>
</task>

<task type="auto">
  <name>Task 2: Update orchestrator with voice profile selection logic</name>
  <files>skills/orchestrator/SKILL.md</files>
  <read_first>
    - skills/orchestrator/SKILL.md
    - references/voice-profiles/voice-profile-spec.md
  </read_first>
  <action>
Update `skills/orchestrator/SKILL.md` Section 2 (On Trigger: Detect or Create Project), subsection "Creating a New Project". Replace the current voice profile handling (steps 1 and 4) with expanded logic covering all four voice input modes. The current step 1 gathers "Voice profile: `spiritual-default` or path to a custom `.md` profile. Default: `spiritual-default`" and step 4 says "Copy the voice profile."

**In step 1** (Gather project details), replace the voice profile line with:
```
- Voice profile (one of four options):
  - **Named profile**: A profile name from the plugin's voice library (e.g., "spiritual-default"). Looks up `${CLAUDE_PLUGIN_ROOT}/references/voice-profiles/[name].md`
  - **Custom file path**: An absolute or relative path to a `.md` voice profile file (e.g., "~/my-voices/academic.md")
  - **Inline description**: A plain-text description of the desired voice (e.g., "casual, conversational, like talking to a friend over coffee"). Will be expanded into a full profile.
  - **Not specified**: Defaults to `spiritual-default`
```

**Replace step 4** (Copy the voice profile) with a new section titled "### Voice Profile Selection" inserted after step 3 (Populate book-dna.md) and before step 5 (Confirm creation). Renumber remaining steps. Content:

```markdown
### Voice Profile Selection

Determine which voice input mode the user specified and process accordingly:

**Mode 1: Named profile** (user provides a name like "spiritual-default"):
1. Read `${CLAUDE_PLUGIN_ROOT}/references/voice-profiles/[name].md`
2. If file not found, list available profiles from `${CLAUDE_PLUGIN_ROOT}/references/voice-profiles/` (exclude `voice-profile-spec.md`) and ask user to choose
3. Copy file contents to `[project]/voice-profile.md`

**Mode 2: Custom file path** (user provides a file path ending in `.md`):
1. Read the file at the provided path
2. Validate against `${CLAUDE_PLUGIN_ROOT}/references/voice-profiles/voice-profile-spec.md`:
   - Check for required sections: Tone, Sentence Patterns, Vocabulary (with Use and Avoid subsections), Emphasis Techniques, Anti-Patterns
   - If any required section is missing: WARN the user which sections are missing, fill each missing section with "[Not specified -- using neutral, clear prose]" marked with <!-- DEFAULT -->
3. Write the (possibly augmented) profile to `[project]/voice-profile.md`

**Mode 3: Inline description** (user provides plain text that is not a file path):
1. Generate a full voice profile from the user's description by expanding it into the required sections:
   - **Tone**: Infer from the description (e.g., "casual" → "Relaxed, conversational, approachable")
   - **Sentence Patterns**: Infer rhythm (e.g., "casual" → shorter sentences, contractions, informal fragments)
   - **Vocabulary > Use**: Extract characteristic language from the description
   - **Vocabulary > Avoid**: Infer what breaks the described voice
   - **Emphasis Techniques**: Infer from the tone
   - **Anti-Patterns**: Infer what would violate the described voice
2. Mark each section with <!-- INFERRED --> to indicate it was generated, not user-specified
3. Write the generated profile to `[project]/voice-profile.md`
4. Show the generated profile to the user: "I've expanded your voice description into a full profile. Here's what I've inferred -- let me know if you'd like to adjust anything before we continue."

**Mode 4: Not specified** (user did not mention voice):
1. Use `${CLAUDE_PLUGIN_ROOT}/references/voice-profiles/spiritual-default.md`
2. Copy to `[project]/voice-profile.md`
3. Inform the user: "Using the default spiritual/theological voice profile. You can change this later by providing a different voice profile."
```

Do NOT change any other section of the orchestrator. Preserve all existing content in sections 1, 3, 4, 5, 6, 7, 8 exactly as they are. Only Section 2 is modified.
  </action>
  <verify>
    <automated>grep -c "### Voice Profile Selection" skills/orchestrator/SKILL.md && grep -c "Inline description" skills/orchestrator/SKILL.md && grep -c "INFERRED" skills/orchestrator/SKILL.md && grep -c "voice-profile-spec.md" skills/orchestrator/SKILL.md && grep "## 3. Pipeline State Detection" skills/orchestrator/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/orchestrator/SKILL.md contains "### Voice Profile Selection" heading
    - skills/orchestrator/SKILL.md contains "Mode 1: Named profile" text
    - skills/orchestrator/SKILL.md contains "Mode 2: Custom file path" text
    - skills/orchestrator/SKILL.md contains "Mode 3: Inline description" text
    - skills/orchestrator/SKILL.md contains "Mode 4: Not specified" text
    - skills/orchestrator/SKILL.md contains "<!-- INFERRED -->" reference for inline mode
    - skills/orchestrator/SKILL.md contains "voice-profile-spec.md" reference for validation
    - skills/orchestrator/SKILL.md still contains "## 3. Pipeline State Detection" (proving other sections preserved)
    - skills/orchestrator/SKILL.md still contains "## 8. Reference File Paths" (proving end of file preserved)
    - skills/orchestrator/SKILL.md still contains "## 1. Pipeline Overview" (proving beginning preserved)
  </acceptance_criteria>
  <done>Orchestrator handles all four voice input modes: named profile, custom file, inline description, and default. Custom files are validated against the spec. Inline descriptions are expanded into full profiles.</done>
</task>

</tasks>

<verification>
- voice-profile-spec.md exists and defines required sections 1-5 and optional sections 6-7
- spiritual-default.md has validation comment confirming spec compliance
- Orchestrator Section 2 has "Voice Profile Selection" subsection with 4 modes
- Orchestrator sections 1, 3-8 are unchanged from Phase 1 output
</verification>

<success_criteria>
- Voice profile spec establishes the contract all profiles must follow
- Default spiritual profile is verified compliant
- Orchestrator can accept named profiles, custom file paths, inline descriptions, or default to spiritual-default
- Custom profiles are validated against the spec with warnings for missing sections
- Inline descriptions are expanded into full profiles with <!-- INFERRED --> markers
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-system-book-outliner/02-01-SUMMARY.md`
</output>
