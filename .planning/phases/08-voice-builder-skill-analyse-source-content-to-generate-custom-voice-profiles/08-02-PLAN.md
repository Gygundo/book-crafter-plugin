---
phase: 08-voice-builder-skill-analyse-source-content-to-generate-custom-voice-profiles
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - skills/orchestrator/SKILL.md
autonomous: true
requirements:
  - VB-04

must_haves:
  truths:
    - "Orchestrator offers 'Build from source material' as a fifth voice selection option"
    - "Selecting Mode 5 invokes the voice builder skill, then resumes pipeline with the generated profile"
    - "Existing four voice selection modes (named, custom path, inline, default) still work unchanged"
  artifacts:
    - path: "skills/orchestrator/SKILL.md"
      provides: "Mode 5 voice selection option invoking voice-builder skill"
      contains: "Mode 5"
  key_links:
    - from: "skills/orchestrator/SKILL.md"
      to: "skills/voice-builder/SKILL.md"
      via: "Invocation during voice selection"
      pattern: "voice-builder"
---

<objective>
Add Mode 5 ("Build from source material") to the orchestrator's voice selection, invoking the voice-builder skill and then resuming the pipeline with the generated profile.

Purpose: Users can trigger voice profile generation directly from the book creation flow without needing to invoke the voice builder separately first.

Output: Updated `skills/orchestrator/SKILL.md` with a fifth voice selection mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-voice-builder-skill-analyse-source-content-to-generate-custom-voice-profiles/08-CONTEXT.md
@.planning/phases/08-voice-builder-skill-analyse-source-content-to-generate-custom-voice-profiles/08-01-SUMMARY.md

@skills/orchestrator/SKILL.md
@skills/voice-builder/SKILL.md

<interfaces>
<!-- Orchestrator voice selection currently has 4 modes (lines ~94-123 of SKILL.md) -->

Current voice selection modes in orchestrator:
- Mode 1: Named profile (user provides name like "spiritual-default")
- Mode 2: Custom file path (user provides .md path)
- Mode 3: Inline description (user provides plain text)
- Mode 4: Not specified (defaults to spiritual-default)

The voice selection section is in "### Voice Profile Selection" under "## 2. On Trigger: Detect or Create Project".

Mode 5 needs to be inserted BEFORE Mode 4 (not specified) since Mode 4 is the fallback when nothing is specified.

Voice builder outputs to: `${CLAUDE_PLUGIN_ROOT}/references/voice-profiles/[auto-name].md`
After voice builder approval, the orchestrator copies the profile to `[project]/voice-profile.md` just like Mode 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mode 5 to orchestrator voice selection</name>
  <files>skills/orchestrator/SKILL.md</files>

  <read_first>
    - skills/orchestrator/SKILL.md (the file being modified -- read the entire Voice Profile Selection section to understand current structure)
    - skills/voice-builder/SKILL.md (the skill being invoked -- understand what it produces and how the approval gate works)
  </read_first>

  <action>
Edit `skills/orchestrator/SKILL.md` to add Mode 5 in the Voice Profile Selection section. Insert it between Mode 3 (inline description) and Mode 4 (not specified).

**Update the voice selection options list** at the top of the "Creating a New Project" section (around line 66) to add:
```markdown
     - **Build from source material**: A directory path containing .md files to analyse. The voice builder skill generates a profile from the source content.
```

**Add Mode 5 block** between Mode 3 and Mode 4 (per D-14):
```markdown
**Mode 5: Build from source material** (user wants to analyse existing writing to generate a voice profile):
1. Ask for the directory path containing source material (if not already provided)
2. Invoke the `book-crafter:voice-builder` skill with the directory path
3. The voice builder analyses the content and presents a profile for user review
4. On approval, the voice builder saves the profile to `${CLAUDE_PLUGIN_ROOT}/references/voice-profiles/[auto-name].md`
5. Copy the approved profile to `[project]/voice-profile.md`
6. Continue with pipeline (proceed to outline stage)

Note: The voice builder handles all analysis, review, and saving internally. The orchestrator just invokes it and waits for the approved profile path.
```

**Do NOT modify** Modes 1-4 or any other section of the orchestrator. This is an additive-only change (per RESEARCH.md Pitfall 4).

**Update the voice selection detection logic** (the block that determines which mode to use) to add detection for Mode 5. Add this condition:
- If the user says "build from my writing", "analyse my content", "generate from my files", "build voice profile", or provides a directory path explicitly for voice analysis -> Mode 5
  </action>

  <verify>
    <automated>grep -c "Mode 5" skills/orchestrator/SKILL.md | xargs test 1 -le</automated>
  </verify>

  <acceptance_criteria>
    - skills/orchestrator/SKILL.md contains "Mode 5: Build from source material"
    - skills/orchestrator/SKILL.md contains "book-crafter:voice-builder" reference in Mode 5 section
    - skills/orchestrator/SKILL.md contains "Build from source material" in the voice selection options list
    - Mode 1 text is unchanged (grep for "Mode 1: Named profile" still matches)
    - Mode 2 text is unchanged (grep for "Mode 2: Custom file path" still matches)
    - Mode 3 text is unchanged (grep for "Mode 3: Inline description" still matches)
    - Mode 4 text is unchanged (grep for "Mode 4: Not specified" still matches)
    - Mode 5 appears BEFORE Mode 4 in the file (Mode 5 line number < Mode 4 line number)
  </acceptance_criteria>

  <done>
    Orchestrator supports a fifth voice selection mode that invokes the voice builder skill. All four existing modes remain exactly as they were. Users can now generate a voice profile from source material during the book creation flow.
  </done>
</task>

</tasks>

<verification>
1. `grep "Mode 5" skills/orchestrator/SKILL.md` returns at least 1 match
2. `grep "Mode 1" skills/orchestrator/SKILL.md` still returns matches (not accidentally deleted)
3. `grep "Mode 2" skills/orchestrator/SKILL.md` still returns matches
4. `grep "Mode 3" skills/orchestrator/SKILL.md` still returns matches
5. `grep "Mode 4" skills/orchestrator/SKILL.md` still returns matches
6. `grep "voice-builder" skills/orchestrator/SKILL.md` returns at least 1 match
</verification>

<success_criteria>
- Orchestrator has 5 voice selection modes, Mode 5 invokes voice-builder
- Existing 4 modes are byte-identical to their pre-edit state
</success_criteria>

<output>
After completion, create `.planning/phases/08-voice-builder-skill-analyse-source-content-to-generate-custom-voice-profiles/08-02-SUMMARY.md`
</output>
