---
phase: 04-editor-revision-workflow
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - skills/orchestrator/SKILL.md
  - references/pipeline-stages.md
autonomous: true
requirements: [ITER-02, ITER-03, ITER-04, ITER-05]

must_haves:
  truths:
    - "Orchestrator Stage 4 invokes editor with explicit three-pass sequence and report generation"
    - "After editing completes, orchestrator presents a review gate with approve/revise/read options"
    - "User can request revision of specific chapters with targeted feedback"
    - "Revised chapters trigger adjacency flow checks on immediate neighbours only (one hop)"
    - "Original drafts are copied to revisions/ with version numbering before overwriting"
    - "Pipeline state detection handles Stage 4 IN REVIEW state via revision marker in consistency-report.md"
  artifacts:
    - path: "skills/orchestrator/SKILL.md"
      provides: "Stage 4 execution detail, review gate, revision workflow, state detection updates"
      contains: "Review Gate"
    - path: "references/pipeline-stages.md"
      provides: "Updated Stage 4 description with three passes and review gate"
      contains: "review gate"
  key_links:
    - from: "skills/orchestrator/SKILL.md"
      to: "skills/editor/SKILL.md"
      via: "Stage 4 invokes editor skill with project directory and edit mode"
      pattern: "book-crafter:editor"
    - from: "skills/orchestrator/SKILL.md"
      to: "reports/consistency-report.md"
      via: "reads report to present review summary and detect revision state"
      pattern: "consistency-report"
    - from: "skills/orchestrator/SKILL.md"
      to: "revisions/ch*-v*-draft.md"
      via: "copies drafts to revisions/ before overwriting during revision"
      pattern: "revisions/"
---

<objective>
Extend the orchestrator with detailed Stage 4 execution logic, a review gate for full draft approval, a revision workflow for chapter-level rewrites with adjacency checks, and version management for revision history.

Purpose: The orchestrator is the user-facing pipeline controller. Without Stage 4 detail, the orchestrator cannot invoke the editor, present the draft for review, or handle revision requests. This plan completes the edit-review-revise loop.

Output: Updated `skills/orchestrator/SKILL.md` with Stage 4 detail and revision workflow, updated `references/pipeline-stages.md` with review gate information.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-editor-revision-workflow/04-RESEARCH.md
@.planning/phases/04-editor-revision-workflow/04-01-SUMMARY.md

@skills/orchestrator/SKILL.md
@skills/editor/SKILL.md
@references/pipeline-stages.md

<interfaces>
<!-- The orchestrator needs to know the editor's invocation interface and output structure -->

From skills/editor/SKILL.md (after Plan 01 implements it):
- Invocation arguments: project directory path, edit mode ("full" or "revision"), chapters to edit (optional list)
- Pass 1 output: edited/ch[NN]-pass1.md with VOICE AUDIT metadata block
- Pass 2 output: edited/ch[NN]-pass2.md
- Pass 3 output: edited/ch[NN]-final.md
- Reports: reports/voice-report.md (implicit in pass1 metadata), reports/flow-report.md, reports/consistency-report.md
- Return summary format: "Editing complete for [Book Title]. Chapters edited: [N]. Voice consistency: [status]. Transitions: [X]/[N-1]. Cross-references: [X] validated, [Y] flagged."

From skills/orchestrator/SKILL.md (current Stage 4 section, lines ~362-371):
```
#### Stage 4: Edit (Voice Consistency + Flow)
The editor reads all drafted chapters and performs three passes:
1. Voice consistency
2. Flow/transitions
3. Cross-chapter validation
For books with more than 15 chapters, use chapter-editor subagent with rolling window.
```
This high-level description needs to be expanded with invocation detail, review gate, and revision workflow.

State detection algorithm (current, lines ~130-157):
```
2. Check for edited/ch*-final.md
   -> If count matches outline chapter count: Stage 4 COMPLETE
   -> If count > 0 but less than expected: Stage 4 PARTIALLY COMPLETE
```
This needs a new state: Stage 4 IN REVIEW (edited files exist + revision marker).

Pipeline stages reference (references/pipeline-stages.md):
Stage 4 description needs review gate and revision information added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend orchestrator Stage 4 with editor invocation, review gate, and revision workflow</name>
  <files>skills/orchestrator/SKILL.md</files>
  <read_first>
    - skills/orchestrator/SKILL.md (full file -- must understand existing structure before modifying)
    - skills/editor/SKILL.md (after Plan 01 -- need to know the editor's invocation interface and output format)
    - .planning/phases/04-editor-revision-workflow/04-RESEARCH.md (review gate flow, revision workflow, state detection updates, adjacency detection)
    - .planning/phases/04-editor-revision-workflow/04-01-SUMMARY.md (what Plan 01 built, any deviations)
  </read_first>
  <action>
Modify the orchestrator SKILL.md to expand Stage 4. The existing file has Stages 1-3 already detailed. Stage 4 (currently ~10 lines at lines 362-371) needs to be expanded to match the detail level of Stages 1-3.

**1. Expand Stage 4 section (replace existing `#### Stage 4: Edit` block):**

```
#### Stage 4: Edit (Voice Consistency + Flow + Validation + Review)
```

**Step 1: Invoke the editor**

Invoke the `book-crafter:editor` skill with arguments:
- Project directory path
- Edit mode: "full"

The editor performs three sequential passes:
1. Pass 1: Voice consistency + theological guardrails (each chapter audited against voice profile)
2. Pass 2: Flow/transitions (sequential chapter-pair analysis, only modifies endings/openings)
3. Pass 3: Cross-chapter validation (term index, reference validation, scripture consistency, theme tracking)

For books with 16+ chapters, the editor uses chapter-editor subagents with rolling window for Pass 1. Passes 2 and 3 are always handled by the main editor skill.

**Step 2: Verify editing output**

After the editor returns:
1. Verify `edited/ch[NN]-final.md` files exist for all chapters (count matches outline)
2. Verify `reports/consistency-report.md` exists
3. Read the editor's return summary for the overview metrics

**Step 3: Review gate (ITER-02)**

Present the draft review to the user:

```
## Draft Review: [Book Title]

Your manuscript is ready for review.

### Summary
- **Chapters:** [N]
- **Total words:** [sum of word counts from METADATA blocks in edited files]
- **Voice consistency:** [Clean/Minor/Significant] ([X] issues found, [Y] auto-resolved)
- **Transitions:** [X]/[N-1] transitions smooth
- **Cross-references:** [X] validated, [Y] flagged

### Consistency Report
See: [project_directory]/reports/consistency-report.md

### Options
1. **Approve** -- proceed to formatting (Stage 5)
2. **Revise chapters** -- tell me which chapters need rewriting and what to change
3. **Read full draft** -- I'll compile all chapters for you to read through

Which would you like?
```

**On Option 1 (Approve):** Proceed to Stage 5.

**On Option 2 (Revise chapters -- ITER-03, ITER-04, ITER-05):**

For each chapter the user wants revised:

a. **Version backup (ITER-05):** Before overwriting, copy the current draft to `revisions/`:
   - Scan `revisions/` for existing `ch[NN]-v*-draft.md` files using `ls revisions/ch[NN]-v*-draft.md 2>/dev/null | sort -V | tail -1`
   - If no existing versions, this is v01. If highest is v02, next is v03.
   - Copy `drafts/ch[NN]-draft.md` to `revisions/ch[NN]-v[VV]-draft.md`

b. **Re-invoke writer:** Spawn a chapter-writer subagent with the user's feedback appended to the standard arguments. The writer produces a new `drafts/ch[NN]-draft.md`.

c. **Re-invoke editor in revision mode:** Invoke `book-crafter:editor` with:
   - Project directory path
   - Edit mode: "revision"
   - Chapters to edit: [the revised chapter number]
   The editor runs Pass 1 on the revised chapter, Pass 2 on the revised chapter + its immediate neighbours (one hop only -- ITER-04), and targeted Pass 3 validation.

d. **Update consistency report:** The editor updates `reports/consistency-report.md` with revision results.

e. **Add revision marker:** If revisions are in progress, add `<!-- REVISION IN PROGRESS -->` to the top of `reports/consistency-report.md`. Remove the marker when all requested revisions are complete.

f. **Return to review gate:** After all requested revisions complete, present the updated review summary. The user can approve, request more revisions, or read the full draft.

**On Option 3 (Read full draft):**
Compile all `edited/ch[NN]-final.md` files into a single markdown document and present it to the user (or tell them the file paths to read). Then return to the review gate.

**2. Update state detection algorithm (Section 3):**

Between the existing check 2 (edited files) and check 3 (drafts files), add the IN REVIEW state:

```
2. Check for edited/ch*-final.md
   -> If count matches outline chapter count:
      -> Check reports/consistency-report.md for <!-- REVISION IN PROGRESS --> marker:
         -> If marker present: Stage 4 IN REVIEW (revisions in progress)
         -> If no marker: Stage 4 COMPLETE
   -> If count > 0 but less than expected: Stage 4 PARTIALLY COMPLETE
```

**3. Update the status dashboard (Section 4):**

Add revision status to the Stage 4 display:

```
[~] Stage 4: Editing (editor) -- IN REVIEW
    [x] Voice consistency pass
    [x] Flow/transition pass
    [x] Cross-chapter validation
    [~] Revision requested: Ch 3, Ch 7
```

**4. Add Mode 5: Revision ("revise chapter 3", "rewrite chapters 5 and 7")**

When the user requests revision of specific chapters on an existing project:
1. Detect pipeline state -- verify Stage 4 is COMPLETE or IN REVIEW
2. Parse chapter numbers from the user's request
3. Execute the revision workflow (version backup, re-write, re-edit)
4. Return to review gate

**5. Update error handling (Section 7):**

Add a new error case:

### Revision Without Editing
If the user requests revision but Stage 4 has not yet run:
> "The manuscript hasn't been edited yet. Let me run the editing passes first, and then you can review and request revisions."

  </action>
  <verify>
    <automated>bash -c 'grep -q "Review Gate\|review gate\|Draft Review" skills/orchestrator/SKILL.md && grep -q "REVISION IN PROGRESS\|revision marker\|REVISION" skills/orchestrator/SKILL.md && grep -q "revisions/ch" skills/orchestrator/SKILL.md && grep -q "IN REVIEW\|IN_REVIEW" skills/orchestrator/SKILL.md && grep -q "adjacen\|neighbour\|neighbor\|one hop" skills/orchestrator/SKILL.md && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - skills/orchestrator/SKILL.md Stage 4 section contains "Pass 1", "Pass 2", "Pass 3" invocation sequence
    - skills/orchestrator/SKILL.md contains "Draft Review" presentation block with Summary, Consistency Report link, and 3 Options (Approve, Revise, Read)
    - skills/orchestrator/SKILL.md contains version backup logic with `revisions/ch[NN]-v[VV]-draft.md` pattern and `sort -V | tail -1`
    - skills/orchestrator/SKILL.md contains re-invoke writer step for revision
    - skills/orchestrator/SKILL.md contains re-invoke editor with mode "revision" for revised chapters
    - skills/orchestrator/SKILL.md contains adjacency check reference (one hop, immediate neighbours)
    - skills/orchestrator/SKILL.md state detection has "IN REVIEW" state checking for `<!-- REVISION IN PROGRESS -->` marker
    - skills/orchestrator/SKILL.md status dashboard shows revision-in-progress state
    - skills/orchestrator/SKILL.md contains Mode 5 for revision requests
    - skills/orchestrator/SKILL.md error handling covers "revision without editing" case
  </acceptance_criteria>
  <done>Orchestrator Stage 4 fully detailed with three-pass editor invocation, review gate (ITER-02), chapter-level revision workflow (ITER-03), one-hop adjacency checks (ITER-04), version backup to revisions/ (ITER-05), IN REVIEW state detection, and revision error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Update pipeline-stages.md with Stage 4 review gate and revision details</name>
  <files>references/pipeline-stages.md</files>
  <read_first>
    - references/pipeline-stages.md (current file to update)
    - skills/orchestrator/SKILL.md (after Task 1 -- to ensure consistency with the orchestrator's Stage 4 detail)
  </read_first>
  <action>
Update the Stage 4 section in `references/pipeline-stages.md` to reflect the three-pass pipeline, review gate, and revision workflow. This file is the reference document that describes all pipeline stages -- it must stay in sync with the orchestrator.

**Replace the existing Stage 4 section with:**

```markdown
## Stage 4: Edit (editor skill + chapter-editor subagent)

**Input:** `book-dna.md` + `voice-profile.md` + all `drafts/ch*-draft.md` files
**Output:** `edited/ch01-final.md`, `edited/ch02-final.md`, etc. + `reports/consistency-report.md`
**Parallel:** Partially -- Pass 1 voice audit can use parallel subagents for 16+ chapter books; Passes 2-3 are sequential

Editing passes (sequential):
1. **Pass 1: Voice consistency + theological guardrails** -- audits vocabulary, sentence rhythm, anti-patterns, and theological framework compliance against the voice profile. Normalises drift.
2. **Pass 2: Flow/transitions** -- reads chapter pairs sequentially, ensures endings connect to next openings. Only modifies final/first paragraphs.
3. **Pass 3: Cross-chapter validation** -- builds term index, validates forward/backward references, checks scripture translation consistency, tracks theme development.

Intermediate artefacts: `edited/ch[NN]-pass1.md`, `edited/ch[NN]-pass2.md` (kept for debugging, not used by pipeline state detection)

**Reports:** `reports/consistency-report.md` with per-chapter findings, severity levels, and specific locations.

**Review gate:** After editing completes, the orchestrator presents the consistency report and offers: approve (proceed to Stage 5), revise specific chapters, or read the full draft.

**Revision workflow:** User requests chapter rewrites with targeted feedback. For each revised chapter:
1. Original draft backed up to `revisions/ch[NN]-v[VV]-draft.md` (version auto-incremented)
2. Chapter re-written by writer agent with user feedback
3. Editor re-runs Pass 1 on revised chapter, Pass 2 on revised chapter + immediate neighbours, Pass 3 on affected references
4. Consistency report updated

**Rolling window:** For books with 16+ chapters, Pass 1 uses chapter-editor subagents. Each receives the current chapter plus 500 words overlap from adjacent chapters.
```

**Update the Stage Completion Detection table:**

Replace the Edit row with:
```
| Edit | `edited/` dir has `ch*-final.md` files matching outline chapter count AND no `<!-- REVISION IN PROGRESS -->` marker in `reports/consistency-report.md` | `edited/ch01-final.md` + `reports/consistency-report.md` |
```
  </action>
  <verify>
    <automated>bash -c 'grep -q "review gate\|Review gate" references/pipeline-stages.md && grep -q "Pass 1" references/pipeline-stages.md && grep -q "revision" references/pipeline-stages.md && grep -q "REVISION IN PROGRESS" references/pipeline-stages.md && grep -q "consistency-report" references/pipeline-stages.md && echo "PASS" || echo "FAIL"'</automated>
  </verify>
  <acceptance_criteria>
    - references/pipeline-stages.md Stage 4 section lists three editing passes (Pass 1, Pass 2, Pass 3)
    - references/pipeline-stages.md mentions `reports/consistency-report.md` as output
    - references/pipeline-stages.md contains "review gate" or "Review gate" description
    - references/pipeline-stages.md contains "revision" workflow description with version backup
    - references/pipeline-stages.md Stage Completion Detection table Edit row includes `REVISION IN PROGRESS` marker check
    - references/pipeline-stages.md mentions rolling window for 16+ chapter books
    - references/pipeline-stages.md Input line includes `voice-profile.md`
  </acceptance_criteria>
  <done>Pipeline stages reference updated with complete Stage 4 description including three passes, review gate, revision workflow, and updated completion detection criteria.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Orchestrator Stage 4 matches the editor skill's invocation interface (mode "full"/"revision", project directory)
2. State detection handles COMPLETE and IN REVIEW states correctly
3. Pipeline-stages.md Stage 4 description is consistent with orchestrator and editor implementations
4. Review gate presents three options (approve, revise, read)
5. Revision workflow includes version backup, re-write, re-edit with adjacency, and return to review gate
</verification>

<success_criteria>
- ITER-02: Review gate presents full draft with consistency report and approve/revise/read options
- ITER-03: User can request rewrite of specific chapters with targeted feedback, writer re-invoked
- ITER-04: Revised chapters trigger Pass 2 flow check on immediate neighbours (one hop only)
- ITER-05: Original drafts copied to revisions/ch[NN]-v[VV]-draft.md before overwriting, version auto-incremented
- Pipeline-stages.md Stage 4 consistent with orchestrator implementation
</success_criteria>

<output>
After completion, create `.planning/phases/04-editor-revision-workflow/04-02-SUMMARY.md`
</output>
